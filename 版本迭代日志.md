# 版本迭代日志 · Version Changelog

> **项目**：人类技术演化网络 · 规律挖掘与未来涌现推演
> **维护**：WorkBuddy（自主构建 Agent）
> **说明**：本文件是项目逐日 / 逐次变更的档案，按**时间倒序**（最新在前）排列。
> **数据同源**：本页记录与站点 [`changelog.html`](changelog.html) 由 `assets/pages/changelog_data.js` 渲染，二者内容一致；此处为可读性 Markdown 镜像。后续变更请同步更新 `changelog_data.js` 与本文件。
> **早期版本**（v0.1.0–v0.8.0）日期依据项目文件时间戳与开发记录**重建**；自 2026-09-06 建立变更档案起，每次变更逐条如实登记。

---

## 后续路线（Roadmap）

- **Phase 3 · 链接预测**：✅ 已完成（v0.9.9 方向修正后）——补全 25 条高可信近因前提边；v0.9.8 初版 2883 条因依赖边方向读反已回滚（详见 v0.9.9）。
- **Phase 4 · 站点集成**：✅ 已完成（v0.9.2）——全量科技树可视化。
- **Tier B · 内容补全**：对 1750 年后归属明确的现代技术做 web 研究，补全发明者 / 地点。

---

## 科技树变更原因 · 全程追踪

> 任何改变整张网络结构 / 规模 / 评分体系 / 推演框架的版本，在此写清原因，便于回溯「树为何变成现在的样子」。本表与站点 [`changelog.html`](changelog.html) 的「科技树变更原因」栏目同源，由 `assets/pages/changelog_data.js` 的 `treeChange` 字段驱动（站点栏目为自动汇总，本表为可读镜像，若出现遗漏以站点为准）。

| 版本 | 日期 | 变更 | 影响范围 | 变更原因 |
|---|---|---|---|---|
| v0.9.22 | 2026-09-13 | 修复 · 结构体检与分步检修（一）：四类结构问题基线量化 + 阶段 0/1 执行（F 族 27→8、同名重复 23→0、零前置 8→4） | 主管线技术网络（模型页 / 分析页 / 来龙去脉页所读）：节点 2,288 → 2,265（合并 23 组同名重复节点）；依赖边 6,139 → 6,091；F 族（跨分类且年份跨度 > 800 年）27 → 8；同名重复组 23 → 0；零前置节点 8 → 4（石器 / 用火 / 手势交流 / 天然磁石）；孤立节点 2 → 0；A 族（全部真实前置仅落在泛化学理枢纽内）563 → 562；Ch 族（下游依赖 = 0 且 year < 1900）267 → 260。环 0 / 自环 0 / 重复边 0 / 悬空前置 0 / 年份倒挂 0。新增 tools/audit_net.js / tools/apply_ledger.js / tools/merge_dups.js、audit/ledger.json / audit/baseline.json、《技术网络检修计划.md》。配合登记：纠错页新增 CR-2026-0913-structure-audit | 用户要求按严谨科学研究口径做结构体检与分步检修，不用主观印象作判据，即先有可复核的量化基线、再按统一判定标准逐条处置、全程留痕。体检确认四类系统性结构问题：A 族 563 个节点（全部真实前置仅落在 14 个泛化学理枢纽内，数学一个节点被 637 个节点依赖）、E 族 173 个节点（仅 1 条真实前置且该前置即泛化枢纽，其中 68 个属制造类，机床族与焊接族前置一律写作「数学」）、Ch 族 267 个节点（下游依赖 = 0 且 year < 1900 的历史断链，其中 basic 类 42 个数学分支全为叶子）、F 族 27 条边（跨分类且年份跨度 > 800 年，如「马蹄铁(300)」指向 10 个军事节点，两端无任何传导机制）。四族同源：均出自 tools/gen_run.js 的「时间就近取前驱」规则及后续批量改写，此前的修复都以「逐条查伪边」为切入点，缺少一次面向全库的、可复跑的结构体检，故问题族的规模与分布一直没有量化基线。 |
| v0.9.21 | 2026-09-13 | 修复 · 生成器槽位边收口：同分类「位置式伪边」按生成器复现逐条判定（处置 719 / 删 549 / 补 95 节点真实前置） | 主管线技术网络（模型页 / 分析页 / 来龙去脉页所读）：依赖边 6,549 → 6,139（删 549 / 加 139 / 净 −410）；节点 2,288 不变；受改节点 490 个；位置式同分类槽位边 719 → 170；零前置节点维持 8 个文明起点；环 0 / 年份倒挂 0 / 悬空引用 0 / 重边 0。95 个节点的模板枚举句同步重写，残留引用 0。改动仅落在 dependsOn 行与模板文案句。配合登记：纠错页 CR-2026-0913-positional-template 状态更新为「已修正（全量 · 两轮）」并补记 6 条，17 个页面版本串 20260913d → 20260913e | 用户报「智能割草机器人怎么会衍生出通用疫苗，毫无关联」。查明该边是生成器产物而非人工误写：v0.9.20 的检测器把生成器建模为「1 个槽位 + 按年份取最近 + 用当前排名」，而 `tools/gen_run.js:67-70` 的实际规则是「同分类最近前驱（最多 **2** 个，按 key = year*100000 + 插入序、在生成时刻求值）」。两处盲区导致伪边残留：① 第 2 槽位从未被扫描（当前数据中「同分类排名 = 2」的边有 203 条）；② 排名漂移——后插入的同分类节点会把旧槽位边挤出视野：`smart_mower`(2015) 在生成 `bio_universalvax`(2020) 时是生命类 rank 1，2026-08-25 拆解批次插入 `mower_nav`/`mower_deck`（同为 2015 年）后掉到 rank 3，从此落在检测器之外。故弃用启发式排名检测，改用「生成器复现 + 逐边 diff」的确定性方法收口。 |
| v0.9.20 | 2026-09-13 | 修复 · 依赖网全量重建：生成器「位置式模板伪边」三变体清零（处置 1,142 条 / 删 1,015 条） | 主管线技术网络（模型页 / 分析页 / 来龙去脉页所读）：依赖边 7,328 → 6,549（删 1,015 / 加 236 / 净 −779）；节点 2,288 不变；被删边节点 647 个、被加边节点 115 个；位置式模板边 806 → 152（保留者逐条核验）；无前置节点 9 → 8；环 0 / 年份倒挂 0 / 悬空引用 0。改动仅落在 dependsOn 行（621 插入 / 624 删除，无格式噪声）。配合登记：纠错页 CR-2026-0913-positional-template 状态更新为「已修正（全量）」，17 个页面版本串 20260913c → 20260913d | 用户裁定按「本质与逻辑」全量过一遍、「不要遗漏」。审计显示 tools/gen_run.js 的 nearestEarlier() 只按时间序取前驱，该规则实际产出三族同源伪边：① 严格「时间最近」（B 是 B 所属分类中 year < A.year 的最后一个节点，778 条）；② 同年份变体（B.year == A.year，287 条——此前审计用严格小于比较，整族漏检）；③ 跨分类同年「前沿并列」变体（97 条——因并列年 tie-break 不同而未被 ② 捕获）。三族本质相同：把「当时最新的材料 / 能源 / 信息条目」当成前置，必须清零，否则滞后 / 下游 / 共生统计与来龙去脉页的因果关系持续失真。 |
| v0.9.19 | 2026-09-12 | 功能 · 理论与解释：新增「技术先行、理论后至」覆盖层标注 | 无（节点 2289、依赖边 7340 与任何现有关系均未变动；仅新增覆盖层标注与详情页渲染。新文件 assets/pages/theory_data.js；core.js 新增 buildTheoryBlock；style.css 新增 .th-*；16 页挂载 theory_data.js、版本串统一 20260912t。全库 74/2289 节点会显示该区块） | 用户指出：有些技术在刚使用时没有科学解释，可能完全没有解释，也可能长期使用朴素哲理或神话解释，后来才有科学解释，然后再过几年发现也不是特别科学。复核确认现有依赖网把 dependsOn 硬编码为「前置」并强制 yearInv=0，「技术先用、理论后到」结构上无法表达（如蒸汽机 1769 的 dependsOn 不含热力学 1824，冶金 −5000 的 dependsOn 不含化学 1661）。本轮不改任何边，另建覆盖层把「节点层」与「事后解释」抽出来标注。 |
| v0.9.18 | 2026-09-12 | 修复 · 构想 B 口径修正：前提齐备是「必要条件」而非「充分条件」 | 无（仅改模型页表述与方法日志，不动数据/网络：节点 2289、依赖边 7340 不变） | 用户指出：技术常是多个技术共同导致；且技术条件成熟后，后置技术不一定诞生，可能由社会条件、偶然性催生。复核后判定模型页「Δ 中位仅 0–2 年 ⇒ 前提一旦齐备技术几乎立刻涌现」属过强断言——近零中位实为编年产物（前置集被数学/化学/经典力学等学科背景枢纽主导，「齐备」近乎恒真；且编年惯例把技术年对齐最晚前置年），本文档自身均值 30.8 年、r²≈0.53 亦与「即涌现」矛盾。修正为「必要条件非充分条件」，并补四道门与反例。 |
| v0.9.17 | 2026-09-12 | 修复 · enables 语义错误批量修正：95 个「灰色概念」占位清零（主库 + 全量镜像）+ 正式甄别表落档 | 主管线技术网络：enables 断链引用移除 83 条（data.js 60 处 + techs_extra.js 2 处）、概念占位 95→11；dependsOn 修正 2 条（gunpowder / optics 去 algorithm），依赖边 7342→7340；节点不变 2289。全量管线 graph.json enables 304→206、断链 12；data_full.js 节点 13413→13319、边 17503→17394、概念节点 96→0 | enables 字段长期混写「技术催生技术」与「技术带来的社会影响/应用」两类语义，自动生成批次又按 summary 关键词机械提取下游（trade←帆船/道路/马具、social←互联网/万维网/智能手机、books←造纸/印刷、cooking←用火、hunting←弓箭/火器），这些指向不存在 id 的断链被 dump_data.js 登记为 kind=concept 占位、前端渲染成灰色虚框标签，即「95 个灰色概念」；另一同源残留藏在全量镜像层（phase2_scores.json 96 条 (concept) 占位经 build_full_dataset.js 评分分支进入 data_full.js）。本轮清理主库断链、加固 build 过滤、修正脏 id 与 algorithm 错挂，并把 95 项三分类+处置甄别表落为纠错页可勾选清单。 |
| v0.9.16 | 2026-09-10 | 修复 · 全量管线镜像同步 + 无人机测绘主库漏修 + 史前/上古 12 处负年倒挂考古修正 | 主管线技术网络：边 7348→7340（删 7 条「专化使能当起源前置」概念化硬边 + arch 去 cement）；年份修正 rope/woodworking/hafting/arch/smithing/loom + bld_dronesurvey 2015→2010；全量管线 graph.json 与主管线 0 差异；data_full.js 节点 13392→13413、边 18048→17503 | v0.9.15 的清理只落在主管线数组源头，graph.json/data_full.js 仍残留同源约 622 条陈旧污染边；反向地 bld_dronesurvey 当年的纠错只修了 graph.json、主管线仍是旧模板错误态——两套数据源互为镜像需双向归并。另 12 处含负年倒挂（史前/上古批次 year 占位互相矛盾 + rope 等被当整批 cohort 万能前置）按考古证据修正：绳索约 4.5 万年前（Abri du Maras 直证）/装柄≥20 万年前/木作约 30 万年前（Schöningen 木矛）/砖拱约 前 1800 年/铁器锻造约 前 1000 年/织机约 前 4000 年等，并对 7 条非必要前置做概念影响降级；顺带修复 build_full_dataset 的 dependency 方向混写使全量树边方向统一。 |
| v0.9.15 | 2026-09-09 | 修复 · 网络年份/概念关系修正 + 二阶枢纽逐节点核验结案 + 量子两项研判 | 主管线技术网络：边 7938→7341（删 4 条概念影响化硬边 + 3 条量子簇虚假下游 + 590 条二阶枢纽污染边）；节点不变 2289 | v0.9.14 把 4 处年倒挂与二阶枢纽列为报告项。用户裁定：① 年份改对并全库自查；② 把「后发学科/方法」被当技术前置的 4 条依赖降级为概念影响；③ 二阶枢纽逐个核验、删污染留真实后结案。本轮逐一落地：概念影响化移除硬边、年份/纪元修正、24 枢纽逐节点核验（实证为「同纪元模板整批注入」）、量子两项 web 研判后修正量子簇；以消除 DAG 时间倒挂与批量模板污染，恢复滞后/下游/共生统计与来龙去脉页可信度。 |
| v0.9.14 | 2026-09-09 | 修复 · 主管线网络审计修正 + 模型页 Phase3 待硬件暂存 | 主管线技术网络：边 8129→7938（删 105 条枢纽误接边 + 去重 86 条重复依赖），节点不变 | v0.9.10–11 的枢纽清理只改了全量管线 graph.json / data_full.js，未写回主管线数组源头（techs_extra.js / data.js / techs_more.js），模型页与分析页所读的主管线网络仍残留同一批「万能前置」误接与重复依赖，污染滞后/下游/共生统计。本轮对 2289 节点主管线全量遍历审计后，在数组源头做镜像修正（4 个已定论枢纽下游 105 边移除 + dependsOn/enables 去重 86 条）。 |
| v0.9.13 | 2026-09-09 | 功能 · 技术演化的数学模型页（5 构想 + 十种进阶模型 + 验证硬化） | 无（新增分析页，不改依赖网） | 把技术依赖网从「可视化图谱」提升为「可计算、可验证的数学对象」：5 构想给出涌现/滞后/组合/中心性/三元流动的量化形式，十种进阶模型作为隐性参数「计算化身」，并设立随迭代演进的建模方法日志与验证门禁；属分析能力扩展，不改动底层依赖网结构与节点边。 |
| v0.9.12 | 2026-09-09 | 文档 · 数据口径澄清（Tier B 1915 gap 与预览工具注入属性） | 无（文档/口径澄清） | 澄清 Tier B 1915 gap 真实含义＝候选而非必填（多为程序化子领域节点按设计留空，仅地标性发明值得回填）；并记录内置预览工具向 HTML 注入的 data-page-node-id 属工具噪声、非数据问题，此前被误当未提交改动处理。 |
| v0.9.11 | 2026-09-09 | 修复 · 依赖关系纠错（续）：清理枢纽节点误接下游与悬空边 | 依赖网：删除 4 枢纽节点 104 条误接下游边 + 21 条悬空边，data_full.js 同步重建 | Phase 3 链接预测 / catalog 合并阶段对通用技术名做了过度泛化的前因推断，把二维材料等当作万能前置注入一批共享模板下游（transport/build/info/military），导致来龙去脉页大量无关上游；另有部分边引用了合并后已消失的节点 id。无人机测绘案例是这一系统性偏差的可见症状。 |
| v0.9.10 | 2026-09-09 | 修复 · 依赖关系纠错（无人机测绘）+ 新增纠错总结页 | 依赖网：修正 bld_dronesurvey 入边（删 3 条错误 + 加 7 条正确），data_full.js 同步 | 原 graph.json 中 bld_dronesurvey 入边是 catalog 合并环节产生的错误因果（二维材料 / 工业 4.0 / 摩擦纳米发电均非其前置技术），导致来龙去脉页生成荒谬文案并回溯出 65 项含大量无关节点的上游列表。修正直接前因才能使依赖网可信。 |
| v0.9.8 | 2026-09-08 | 功能 · Phase 3 链接预测（⚠ 写回方向有误，已由 v0.9.9 回滚重写） | 全量依赖网：17837 → 20720 直接依赖边（+2883） | 此前的依赖网稀疏，大量「A 的已知前提都直接依赖 B」的隐含前提关系未显式记录，计划用前提支撑度补全使前置关系更完整。⚠ 但实现时把依赖边方向读反，2883 条写回边方向错乱，v0.9.9 已全部回滚并纠正。 |
| v0.9.4 | 2026-09-08 | 功能 · 未来涌现推演引擎 | 推演层：以确定性引擎替代手工年份，新增 L2/L3/L5 涌现推导 | 为落实校正后目的，不能用臆测年份。从真实网络实算「前提齐备律」滞后分布（year≥1700, n=6684, p50=26/p90=147），按前提闭包推导涌现时期，使科技树「未来枝」由内生规律产生而非外部假设。 |
| v0.9.3 | 2026-09-08 | 重构 · 项目目的校正 | 数据净化：隔离 extend_net.js 手工拍年份的 117+ 未来节点（2050–2199 仅 51 个） | 用户校正目的为「严肃科学、非科幻」。手工臆测的科幻外推年份节点污染推演正确性，明令排除；未来档此后一律由网络前提闭包 + 前提齐备律推导。 |
| v0.9.2 | 2026-09-06 | 功能 · 全量科技树站点集成 | 全量科技树：13392 节点 + 18141 边 接入站点可视化层 | build_full_dataset.js 合并 graph.json+new_techs+phase2_scores，重建依赖/赋能边，使评分卡与网络在站点层可交互，是「树」首次完整呈现给用户。 |
| v0.8.0 | 2026-09-04 | 里程碑 · 图算法 + 五维评分 | 评分体系：13392 节点首次获得五维评分与 L1–L5 五档标记 | 此前树只有节点 + 依赖边，无法量化成熟度。引入第一性原理五维后，树从「有无依赖」升级为「成熟度可量化、可推演未来档」。 |
| v0.3.0 | 2026-09-02 | 数据 · 88 任务域骨架 | 全量网络规模：2362 → 13392 节点（+11031） | 此前树仅覆盖原始库，缺失大量现代技术域。改用 88 任务域骨架 + 子分类流水线批量扩充，使网络从骨架变近全量覆盖。 |
| v0.1.0 | 2026-08-26 | 里程碑 · 静态站点骨架 | 原始技术库：≈2265 条技术 → 依赖图 2362 节点 | 项目启动首次建网：确立 9 大分类与历史分期，搭出 dependency 依赖边骨架。此后所有节点/边均以此扩展，是整张科技树的根版本。 |

---

## v0.9.22 · 修复 · 2026-09-13

### 结构体检与分步检修（一）：四类结构问题基线量化 + 阶段 0/1 执行

用户要求：**「不要给出这种感性的评论……我们在做严谨的科学研究；给检修计划加上日期，放进文件里，同时这几个错误也总结下，计划和错误也增加到日志和纠错里；然后开始按顺序依次执行上面的 0 1 2 3 4 5。」** 据此本轮不做零散修补，改为「先量化基线、再分步检修」：先建立可复跑的全库结构体检器与逐条判定台账，据其编制《技术网络检修计划.md》（含编制日期与逐批状态栏），再按阶段 0 → 5 顺序执行。**本轮完成阶段 0 与阶段 1（止血三批）；阶段 2–5 待执行。**

**一、体检基线（四类结构问题）**

体检不使用主观印象，全部以脚本量化、可复跑（`node tools/audit_net.js`）。

| 族 | 定义 | 基线 | 分类分布 |
|---|---|---|---|
| **A** | 节点的全部真实前置都落在 14 个泛化学理枢纽内 | **563** | basic 176 / material 96 / info 92 / life 80 / manufact 68 / energy 48 |
| **E** | 仅有 1 条真实前置，且该前置即为泛化枢纽 | **173** | info 91 / manufact 68 / life 5 / basic 2 / material 2 / energy 2 / build 2 / military 1 |
| **Ch** | 下游依赖数 = 0 且 year < 1900（历史断链） | **267** | material 44 / basic 42 / energy 34 / transport 33 / manufact 30 / build 26 / military 25 / life 23 / info 10 |
| **F** | 跨分类且年份跨度 > 800 年 | **27** | 逐条列表见计划文档 1.3 节 |

14 个泛化学理枢纽及其下游度：数学 637、化学 433、经典力学 289、算法理论 214、统计学 192、工程学 170、文字 114、数值分析 105、最优化 105、分子生物学 94、材料科学 93、遗传学 89、免疫学 88、语言 12。

附带问题：同名重复节点 23 组、孤立节点 2 个（钻木取火 / 手势交流）、零前置节点 8 个、双管线分叉（`analysis-engine/data/graph.json` 2,299 节点 / 7,539 边，其中 1,689 条边只在该文件、404 条只在主管线）。

**二、判定四标准（全阶段统一口径）**

新增或保留一条「A 是 B 的前置」须同时满足：

1. **必要性**——去掉 A，B 是否仍能按史实出现？仅因同处一个时代或同一大类不成立。
2. **排他性**——A 是否为该环节最直接的先行者？
3. **时序性**——A 的年份早于 B，或同期可达。
4. **域内优先**——跨域须有明确传导机制。

处置分四档：**保留** / **改前置** / **删边** / **降级为概念影响**。

**三、阶段 0 · 工具与门禁**

| 批次 | 产出 | 状态 |
|---|---|---|
| 0.1 | `tools/audit_net.js` 体检器 + `audit/baseline.json` 基线快照与 `--check` 回归门禁 | 2026-09-13 已完成 |
| 0.2 | `audit/ledger.json` 判定台账 + `tools/apply_ledger.js` / `tools/merge_dups.js` 执行器 | 2026-09-13 已完成 |
| 0.3 | `tools/gen_run.js` 生成器护栏（`--no-write` 干跑 + 槽位边上限断言 + 写前备份） | 2026-09-13 已完成 |
| 0.4 | 门禁扩充（由 `audit_net.js --check` 承担；`check_docs.js` 继续管文档数字） | 2026-09-13 已完成 |

`--check` 的 mustZero = 环 / 自环 / 重复边 / 悬空前置 / 年份倒挂；监控不得上升 = A / E / F / 重名组 / 孤立五项计数。

台账制是本轮的核心工程方法：每批次在 `audit/ledger.json` 记录逐条 `verdict` + `reason` + `newParent` + `backfills` + `summary`，驱动器断言「`keep + repoint + delete + concept` 恰好等于候选数」及「`backfills.length` 等于 `summary.backfill`」——**不允许漏判，也不允许统计口径含糊**。

生成器护栏实测：`tools/gen_run.js` 每次运行产生同分类槽位边 **1,065 条**（占 3,899 条的 27.3%）、指向 `mathematics` 的固定槽位边 **458 条**——即模板污染的再生源。默认上限设为 0，未显式加 `--allow-overflow` 时一律拒写，并在写出前自动备份现有产物到 `/tmp`。

**四、阶段 1 · 止血三批**

| 批次 | 范围 | 处置结果 | 状态 |
|---|---|---|---|
| 1a | F 族 27 条跨类长跨度边 | 删 15 / 改前置 4 / 保留 8；补真实前置 10 条 | 已完成 |
| 1b | 4 个无前置的文明起点 + 孤立节点 | 补前置 4 条：零前置 8 → 4、孤立 2 → 0 | 已完成 |
| 1c | 23 组同名重复节点 | 合并 23 组；并入独有前置 7 条、剔除 4 条；引用改指 19 处 | 已完成 |

1a 明细——**删 15**：`inf_pen`←`paper`，以及 `mil_plate` / `mil_arquebus` / `mil_musket` / `mil_pistol` / `mil_bodyarmor` / `mil_landmine` / `mil_rocketartillery` / `mil_cannon` / `mil_grenade`←`tr_horseshoe`（马蹄铁，year 300）、`metamaterial` / `directed_energy` / `mil_directedenergy` / `display`←`optical`（光学，year 1000）；**改前置 4**：`holography`←`wave_optics`，`led` / `lasers` / `laser`←`quantum_mech`；**保留 8**：跨域但有明确传导机制者（如 火药→火箭 / 钻爆法）。补前置 10 条：板甲←钢铁 + 锻造；火绳枪 / 滑膛枪 / 手枪←火药 + 铁器 + 螺纹；防弹衣←板甲 + 钢铁；地雷←火药 + 铁器；火箭弹←火药 + 箭；加农炮←火药 + 青铜铸造；手榴弹←火药；自然采光←浮法玻璃 + 建筑学。

1b 明细：轮子←木作，石器打制←石器，钻木取火←石器 + 用火，语言←手势交流。收敛后全库起点为 **4 个**：石器 / 用火 / 手势交流 / 天然磁石。

1c 明细：保留规则以「具备 `views` 与 `applications` 富字段」为准（详情卡依赖该字段），该类条目年份均不晚于同名冗余项；冗余项全部位于 `techs_extend.js`。并入冗余项独有且时序成立的前置 7 条（`mat_forging`←用火 + 风箱 + 木炭，`ene_charcoal`←用火，`tr_canoe`←用火 + 石器）；4 条并入项因时序性剔除（铁器→`mat_forging`、微波→`ene_wpt`、物联网→`mfg_digitaltwin`、基因编辑→`bio_genetherapy`，均晚于保留项年份）。全库 `dependsOn` 16 处 / `enables` 3 处引用同步改指，残留引用 0。

**五、结果核对**

| 指标 | 前 | 后 |
|---|---|---|
| 节点 | 2,288 | **2,265** |
| 依赖边 | 6,139 | **6,091** |
| F 族 | 27 | **8** |
| 同名重复组 | 23 | **0** |
| 零前置节点 | 8 | **4** |
| 孤立节点 | 2 | **0** |
| A 族 | 563 | 562 |
| Ch 族 | 267 | 260 |
| 环 / 自环 / 重复边 / 悬空前置 / 年份倒挂 | 0 | **0** |

全部写盘经五道门：台账完整划分断言 → 时序性校验 → 环校验 → 逐节点写后断言 → 残留引用检查；三批逐批通过。

**六、遗留与后续（阶段 2–5，待执行）**

- **阶段 2**：96 条中间技术（`techs_midtech.js` 无 `category` / `dependsOn`，仅挂 `midtech.html`）分批入网；A 族 562 个节点补领域前置。
- **阶段 3**：E 族 173 个节点补前置，重点是制造类 68 个的机床族与焊接族。
- **阶段 4**：Ch 族 260 个历史断链补下游依赖（basic 类 42 个数学分支全为叶子）；分类归属复核。
- **阶段 5**：双管线归并（1,689 / 404 条单向差、11 个概念占位含脏 id `calendar_agri`）与生成器侧同步。

**经验教训（已写入项目记忆备查）**：改写 `dependsOn` 时，引号风格的判定必须回退到文件级探测——当原数组为空时「依据数组内容判引号」会失效并写出无引号 id，导致 `ReferenceError`；本轮即因此写脏一次，已从 `/tmp` 备份回滚并修正判据。

**科技树变更原因**
- **影响范围**：主管线技术网络（模型页 / 分析页 / 来龙去脉页所读）：节点 2,288 → 2,265；依赖边 6,139 → 6,091；F 族 27 → 8、同名重复组 23 → 0、零前置 8 → 4、孤立 2 → 0、A 族 563 → 562、Ch 族 267 → 260。
- **原因**：四族问题同源，出自 `tools/gen_run.js` 的「时间就近取前驱」规则及后续批量改写。此前修复都以「逐条查伪边」为切入点，缺少一次面向全库、可复跑的结构体检，故问题族的规模与分布一直无量化基线。本轮先建基线再分批处置，使每一步都可用 `--check` 回归验证。
- **具体动作**：新建 `tools/audit_net.js`（体检器）、`tools/apply_ledger.js` 与 `tools/merge_dups.js`（台账执行器）、`audit/ledger.json`（判定台账）、`audit/baseline.json`（基线快照）、`技术网络检修计划.md`（分阶段计划）；`tools/gen_run.js` 增加 `--no-write` 干跑开关、同分类槽位边上限断言与写前自动备份。

**涉及文件**：`assets/techs_extra.js`、`assets/techs_extend.js`、`assets/techs_more.js`、`assets/data.js`、`技术网络检修计划.md`、`tools/audit_net.js`、`tools/apply_ledger.js`、`tools/merge_dups.js`、`tools/gen_run.js`、`audit/ledger.json`、`audit/baseline.json`、`assets/pages/changelog_data.js`、`assets/pages/correction_data.js`

---

## v0.9.21 · 修复 · 2026-09-13

### 生成器槽位边收口：改用「生成器复现 + 逐边 diff」的确定性方法

用户提问：**「智能割草机器人怎么会衍生出通用疫苗，毫无关联，检查下是什么问题。」** 结论是——这条边不是人手写错的，是**生成器造的**；而且它躲过了 v0.9.20 的清洗，暴露出检测器建模的一处系统性盲区。

**根因（逐行可查）**

`tools/gen_run.js` 第 67-70 行：

```js
// 同分类最近前驱（最多 2）
const same = (byCat[n.cat] || []).filter(t => t.key < T.key && t.id !== n.id);
let cnt = 0;
for (let i = same.length - 1; i >= 0 && cnt < 2; i--) { deps.add(same[i].id); cnt++; }
```

对每个新节点，无条件把**同分类中时间最近的 1–2 个节点**当前置，零语义判断。`bio_universalvax`（通用疫苗，life/2020）因此被接上了 `smart_mower`（智能割草机器人，life/2015）—— 它只是「当时生命类里最新的节点之一」。

**复现证据（确定性，非启发式）**

把 `tools/gen_techs.js` + `tools/gen_run.js` 复制到隔离目录（**铁律：绝不可在仓库内直跑——`gen_run.js` 结尾的 `fs.writeFileSync` 会覆盖 `assets/techs_extra.js`**），锚点源换成拆解前快照 `assets/data.before_disasm.js`，并在依赖分配三处插入 `ORIGIN[子>前]` 标签后复跑：

```
复现：anchors 112 / newTechs 992 / 总 1,104 节点 / 3,900 边 / 环无
bio_universalvax.dependsOn = ["chemistry","mathematics","smart_mower","bio_crispr"]
```

与仓库里的原始生成文件 `assets/techs_extra.backup.js` **完全一致**。来源标签统计：

| 来源 | 生成 | 当前仍存活 |
|---|---|---|
| `prereq`（跨类前提槽） | 2,834 | 1,137 |
| `slot1`（同分类最近 1） | 989 | 551 |
| `slot2`（同分类最近 2） | 983 | 168 |

用户报的那条边，标签就是 `slot1`。

**为什么躲过了 v0.9.20**

| | 检测器（v0.9.20） | 生成器（实际规则） |
|---|---|---|
| 槽位数 | 1 个 | **最多 2 个** |
| 排序依据 | 按**年份**取最近 | 按 `key = year*100000 + 插入序` |
| 求值时刻 | 用**当前**排名 | 在**生成时刻**求值 |

两处盲区：

1. **第 2 槽位从未被扫描** —— 当前数据中「同分类排名 = 2」的边有 **203 条**。
2. **排名漂移** —— 后插入的同分类节点会把旧槽位边挤出视野。`smart_mower` 生成时是生命类 rank 1；2026-08-25 的拆解批次插入 `mower_nav` / `mower_deck`（id 索引 1129 / 1128，同为 2015 年）后，它掉到 **rank 3**，从此落在检测器之外，页面上就显示成「智能割草机器人 ——衍生→ 通用疫苗」。

（同节点的另一半 `bio_crispr` 因该节点被整体删除而消失，纯属巧合，与清洗无关。）

**全量收口**

候选集 =「生成器同分类槽位边（slot1 + slot2）且当前仍存活」= **719 条**。判定口径：**A 是否真的建立于 B 之上**；仅时间相邻、或同域但互为并列者一律判删。

分域候选数与删除后的结果：

| 分类 | 候选 | 分类 | 候选 |
|---|---|---|---|
| basic | 58 | manufact | 86 |
| build | 53 | material | 116 |
| energy | 67 | military | 7 |
| info | 95 | transport | 57 |
| life | 180 | | |

**处置 719 条 = 删 549 条 + 保留 170 条**（完整划分，无越界项）。

删除样例（位置式伪边）：

- 农业：`bio_dentistry←犁`、`bio_acupuncture←犁`、`bio_chemo←益生菌`、`bio_enzyme←核医学`、`bio_mri←基因工程`
- 信息：`inf_ssd←万维网`、`inf_hash←彩色电视`、`inf_db←激光器`、`inf_compiler←强化学习`、`inf_cache←负载均衡`
- 交通：`tr_escalator←驱逐舰`、`tr_hyperloop←平衡车`、`tr_icebreaker←人造卫星`、`tr_aircraftcarrier←叉车`、`tr_lunarlander←自主水下航行器`
- 建筑：`防水←灌浆`、`自然采光←保温隔热`、`通风←自然采光`、`给排水←通风`（1800 年退化链）
- 材料：`mat_porcelain←造纸`、`mat_lead←青铜`、`mat_stainless←低温超导`、`mat_pe←气凝胶`

保留样例（真实前置）：`counting←language`、`numerals←counting`、`cartography←arithmetic`、`telescope←observ_astron`、`kepler_laws←telescope`、`calculus←physics`、`probability←combinatorics`、`atomic_theory←spectroscopy`、`computer_sci←lambda_calc`、`control_theory←info_theory`、`bld_adobe←bld_rammed`、`bld_gothic←bld_flying`、`bld_shield←bld_underground`、`mfg_handaxe←mfg_stone`、`mfg_lostwax←mfg_sandcast`、`mfg_lathe←iron_smelting`、`mfg_thread←mfg_bolt`、`mat_iron←iron`、`mat_quench←mat_heat`、`mat_temper←mat_quench`、`mat_2d←mat_graphene`、`mil_arrow←mil_bow`、`mil_sniper←mil_rifle`、`tr_lighthouse←tr_navigation`、`tr_simulator←airplane`、`tr_passdrone←tr_cargo_drone`。

**补写 95 个被清空节点的真实前置**

删边后有 95 个节点会**失去全部前置**（如 牙科 原只依赖「犁」、自动扶梯 只依赖「驱逐舰」、防水 只依赖「灌浆」、平板玻璃 只依赖「高炉炼铁」），逐个人工补写领域前置：

```
bio_dentistry ← [草药学, 石器]        bio_herb ← 农业
ene_hydro ← 轮子                      ene_coal ← 石器
bld_timber ← [木作, 土坯砖]           bld_tile ← [窑, 陶器]
bld_stone ← [砖石砌筑, 石器]          bld_hvac ← [热力学, 铸铁]
bld_variabletbm ← 盾构法              bld_carbonconc ← [碳纤维复合材料, 混凝土]
mat_gold ← 锤                         mat_casting ← 陶器
mat_zinc ← 铅冶炼                     mat_blastfurnace ← 冶铁
mat_porcelain ← [窑, 炻器]            tr_steamloco ← 蒸汽机
tr_destroyer ← [蒸汽船, 鱼雷]         tr_radar2 ← [无线电, 电磁学]
tr_jetliner ← 喷气发动机              tr_evcar ← [电池, 电动机]
tr_hsr ← [铁路, 电力传输]             tr_hyperloop ← 高速铁路
```

写入前做 id 存在性 + 年份倒挂校验：**95/95 覆盖、引用 id 全部存在、0 倒挂、0 重复、应用后环 0**。

**文案一致性**

96 个节点的正文仍在复述已删前置。按句式定位后改写 **95 个**（模板枚举句：「依托 A、B 把 … 落到实处」「建立在 … 之上」「依托 A 解决了 …」「把 A 与 … 连接起来」），按新 `dependsOn` 重写，**残留引用 0**：

```
牙科      「依托 数学、犁、草药学 把 …」 → 「依托 草药学、石器 把 …」
水力利用  「依托 用火、木炭 把 …」       → 「依托 轮子 把 …」
```

第 96 个为传真机——「摄影」出现在手写史实叙述（「靠电化学记录与摄影制版」）中，不属模板枚举句，判定为**误报**，未改。

**核对结果**

```
节点          2,288（不变）
依赖边        6,549 → 6,139（删 549 / 加 139 / 净 −410）
受改节点      490
重边          0
自环          0
悬空前置      0
年份倒挂      0
环            0
零前置节点    8（language / stone_tools / fire / wheel / mfg_stone / fire_drill / gesture_comm / magnet）
位置式槽位边  719 → 170
```

校验链：`/tmp/check_net.js` 全绿；`tools/check_docs.js` [OK]（TOTAL 2288 / TOTAL_FULL 13318）；模型页 runtime 干跑无残留模板串（P 1.33 / R² 0.874、K 2.04 / R² 0.932）；依赖徽章干跑（钢笔 6 枚 / 控制论 4 枚含 2 虚边 / 巴拿马运河 4 枚 / 传真机 3 枚 / 充气轮胎 4 枚；兜底蒸汽机 8 枚）；纠错页干跑 9/9；版本页干跑末条 = v0.9.21。

**方法沉淀**

1. **位置式伪边不能用启发式排名检测收口**——启发式永远追不上排名漂移（节点持续插入会让旧槽位掉出视野）。应改用**生成器复现 + 与当前数据逐边 diff**：把生成器在隔离目录重跑、给每条边打来源标签，即可拿到完整且确定的候选集。
2. **生成器脚本不可在仓库内直跑**——`gen_run.js` 结尾会写盘覆盖 `assets/techs_extra.js`；复现必须复制到临时目录，并把锚点源换成历史快照。
3. **「只删不连」之后必须补前置**——本轮 490 个受改节点里有 95 个会因删边而失去全部前置；不补的话节点会变成无源孤点。
4. **文案按句式定位改写，不要全文替换**——手写史实叙述里可能出现与前置同名的词（传真机的「摄影」），只改模板枚举句才不会误伤。
5. **判定落盘为完整划分**——719 条候选逐条给出去向（删 / 留），脚本断言「候选 = 删 + 留」，任何未覆盖项立即报错，避免静默遗漏。

**遗留（下一批）**

1. **264 个节点删边后仅剩泛化枢纽前置**（数学 / 化学 / 文字）——这是本轮「诚实但单薄」的代价：删掉荒谬边之后，这些节点在「来龙去脉」页只剩学科枢纽可展示，需逐个人工补写领域前置。**列为下一批首要任务。**
2. 重写 1027 条 `upgrade_stubs` 三段模板文案。
3. 给 `tools/gen_techs.js` · `gen_run.js` · `upgrade_stubs.js` 加语义硬约束防再生（管线未加锁，重跑会复现伪边）。
4. 22 组同名节点去重。
5. 12 条 `enables` 悬空引用。
6. 部分节点「直接催生了 X、Y」列表仍与当前反图不一致（反向索引陈旧，非本轮改动所致）。

---

## v0.9.20 · 修复 · 2026-09-13

### 依赖网全量重建：生成器「位置式模板伪边」三变体清零

用户指令：**「这个纠错很重要，也加到日志和纠错里面；然后把剩下的所有都过一遍，不要遗漏，然后推动。」** 本轮把 v0.9.19 之后遗留的模板伪边全量清完，并把方法沉淀进文档。

**为什么会有「三族」——审计规则的漏洞**

`tools/gen_run.js` 的 `nearestEarlier(cat, key)` 只按时间序取前驱（取该分类中「当时最新」的节点），零语义判断。最初审计只试了严格版规则（`B.year < A.year`），于是漏掉了两族变体：

| 族 | 判定 | 条数 | 处置 |
|---|---|---|---|
| ① 严格「时间最近」 | B 是 B 所属分类中 `year < A.year` 的最后一个节点 | 778 | 保留 106 / 删 672 |
| ② 同年份变体 | `B.year == A.year` 且 B 是该分类截至 A 年的最新节点 | 287 | 保留 29 / 删 258 |
| ③ 跨分类同年「前沿并列」 | B 与 A 同年，且 B 是该分类截至 A 年的最新年份节点**之一** | 97 | 保留 17 / 删 80 |
| | | **1,142** | **保留 152 / 删 1,015** |

② 之所以整族漏检，是因为先前审计用的是严格小于比较（`ne()` 里的 `a[i].y < year`），同年恒不命中；③ 则是并列年 tie-break 不同——同一个「槽位」在某年有多个并列候选时，生成器与检测器选中的不是同一个（例：`mil_sword`(−3000) 拿到 `研磨` 与 `渡轮`，而检测器认为槽位是别的节点）。

**保留的 152 条为什么站得住**

- **同分类 95 条＝「本领域上一节点」或「系统←自身部件」**：`steam_engine←boiler`、`mat_wrought←mat_iron`、`gunpowder←mil_blackpowder`、`www←web_server`、`internet←packet_switch`、`llm←transformer_arch`、`smartphone←camera_module`、`tunnel←drill_blast`、`internal_combustion←otto_cycle`。
- **跨界 57 条＝真实材料 / 动力 / 工艺输入**：`mfg_bolt / mfg_thread / tr_crane←mat_blastfurnace`、`inf_mimeograph / tr_windtunnel / mfg_injection←mat_toolsteel`、`airplane←aircraft_engine`、`tr_rocket / liquid_rocket←ene_rocketengine`、`mfg_sandcast / mfg_lostwax←mat_casting`、`transistor / inf_ram←semiconductors`、`mil_aweapon←ene_nuclear`。

**删除的 1,015 条长什么样**

`inf_hash(1953)←聚碳酸酯(1953)`、`Python(1991)←锂离子电池(1991)`、`mil_sam / mil_cruise / mil_inertial←超声加工 / 遥控潜水器`、`mil_sword / mil_shield / mil_armor←[研磨, 渡轮, 水力利用, 水泥]`、`bld_green / bld_passive / bld_verticalgreen←在机测量`、`mil_rifle / mil_sniper / mil_recon / mil_logistics / mil_fieldhospital / mil_assaultbridge / mil_camo←管道运输(1800)`、`mil_detonator(雷管)←地铁(1863)`。

**补写的 97 个节点**（被清空后整表重写；写入前一律做 id 存在性 + 年份倒挂校验）

- 交通：`tr_metro←[蒸汽机车, 交通隧道]`、`tr_maglev←[直线电机, 低温超导]`、`tr_motorcycle←[自行车, 内燃机, 钢铁]`、`tr_airship←[热气球, 蒸汽机]`、`tr_submarine←[桨, 铆接]`、`tr_bulktanker←[蒸汽机, 钢铁, 石油工业]`、`tr_charging←[电网, 锂离子电池, 电动机]`。
- 军事：`mil_rifle←[滑膛枪, 镗床]`、`mil_machinegun←[来复枪, 铣床, 钢铁]`、`mil_ballistic←[火箭, 制导技术]`、`mil_cruise←[弹道导弹, 喷气发动机]`、`mil_hweapon←[原子弹, 核裂变反应堆]`、`mil_nuclearsub←[潜艇, 核裂变反应堆]`、`mil_triad←[弹道导弹, 潜艇]`、`mil_aewc←[飞机, 雷达, 无线电]`、`mil_sonar←[声学, 电子学]`、`mil_railgun←[电磁学, 超级电容]`。
- 建造：`bld_immersed←[钢筋混凝土, 挖泥船]`、`bld_pump←[钢筋混凝土, 内燃机, 起重机]`、`bld_zeroenergy←[太阳能光伏, 太阳能热利用, 地源热泵建筑]`、`bld_curtain←[钢化玻璃, 钢结构]`、`bld_isolation←[天然橡胶, 钢结构]`、`bld_sprinkler←[消防系统, 管道运输]`。
- 材料 / 工艺：`mat_brass←[红铜冶炼]`、`mat_stoneware←[窑, 陶器]`、`mat_mercury←[窑, 陶器]`、`mat_concrete←[石灰, 炻器]`、`mat_refractory←[炻器, 窑]`；`sail←[桨, 编织, 木作]`。
- 文明起点保持空：`language / stone_tools / fire / wheel / mfg_stone / fire_drill / gesture_comm / magnet`（另恢复了一处误删 `rope←stone_tools`——该节点简介明写「建立在石器之上」，且属「唯一候选槽位」）。

**环与倒挂归零（含一处回归修复）**

补前置过程中，`mat_brass` 补入的 `mat_zinc` 经 `mat_castiron→mat_anneal→mat_temper→mat_quench→mat_heat→mat_wrought→iron→mat_refractory←mat_brass` 形成 **8 个环**。改为只保留 `[红铜冶炼]`（**黄铜＝铜＋锌矿**，金属锌出现晚于黄铜，年代与语义都不支持该边）后环归零。另 `bld_curtain(1950)←浮法玻璃(1952)` 产生年份倒挂，改用钢化玻璃后归零。

**核对结果**

```
节点 2288（不变）| 边 7,328 → 6,549（删 1,015 / 加 236 / 净 −779）
重边 0 | 自环 0 | 悬空前置 0 | 年份倒挂 0 | 环 0
位置式模板边 806 → 152（保留者逐条核验）| 无前置节点 9 → 8
改动形态：仅 dependsOn 行，621 插入 / 624 删除，无格式噪声
校验链：check_docs [OK]（TOTAL 2288 / TOTAL_FULL 13318）
        模型页 runtime 干跑（无残留模板串）| 前置性质渲染干跑（钢笔 6 枚 / 控制论 4 枚含 2 虚边 / 巴拿马运河 4 枚）| 纠错页干跑 9/9
```

**方法沉淀（写进纠错页，供复用）**

1. **位置式判定规则只能作筛查信号，不能作判据。** 新采用的前置里只有极少数恰好命中「时间最近」启发式，而正确边（如 电话→电话交换机）反会被该启发式误判。
2. **「同年」同样不能当判据。** 全库同分类同年仍有 654 条边，属正常「系统←自身部件」关系（`www←http`、`smartphone←soc`、`tunnel←盾构机`）。
3. **「只删边、不逐个重连」优于逐条重构**——避免在清理过程中引入新的误连；被清空的节点再单独补写。
4. **同类规则会产生「并列年 tie-break」的隐性变体**，审计时必须同时试 `<` 与 `<=` 两种比较，并检查「前沿并列集合」。
5. **compact 格式陷阱**：`assets/data.js` 里的锚点节点依赖数组元素**不带引号**（是 JS 标识符引用）。改写必须原样保留 token 文本，曾因重新加引号触发 `ReferenceError: mathematics is not defined`，已回滚改用原文本保留。

**本轮未动（已登记为下一批）**

1. **1027 条 `upgrade_stubs.js` 三段模板文案**——本轮只改边、未改文本，故部分节点简介仍写着已删掉的旧前置（如 `inf_pencil` 简介仍称「依托 锆、蒸汽机、数学」）。口径：按新 `dependsOn` 重写、不绑定具体边、不得编造。
2. 给 `tools/gen_techs.js · gen_run.js · upgrade_stubs.js` **加语义硬约束**，防止再生（当前只做了手工清理，管线未加锁，重跑工具会复现）。
3. 其余 **22 组同名节点**去重；**12 条 `enables` 悬空引用**（已在纠错页 `enablesAudit` 逐条列明处置建议）。

---

## v0.9.19 · 功能 · 2026-09-12

### 理论与解释：新增「技术先行、理论后至」覆盖层标注

用户指出：**有些技术在刚使用时没有科学解释——可能完全没有，也可能长期使用朴素哲理或神话解释，后来才有科学解释，然后再过几年发现也不是特别科学。** 要求把这类型论性的内容「抽离出来，但是又合理标注」。

**复核：现有网络为什么表达不出这个现象**

- `dependsOn` 的语义被硬编码为「前置」，且自 v0.9.16 起 `yearInv = 0` 强制「前置年 ≤ 技术年」。于是「技术先用、理论后到」**在结构上不可能存在**。
- 实测：`steam_engine`(1769) 的 dependsOn 为 煤炭/铁器/钢铁/纽科门机/瓦特机/锅炉——**不含 热力学(1824)**；`metallurgy`(−5000) 为 采矿/用火——**不含 化学(1661)**；`mat_glass`(−2500)、`fermentation`(−7000)、`vaccination`(1796，不含更晚的免疫学) 同样。
- 全库 2289 条 summary 中，命中「偶然/意外/巧合」= 0 条，也**没有任何一条标注「长期靠朴素理论解释」**。
- 规模：`basic`（基础）分类 304 个是混装桶——真节点 144 + `bx_*` 填充占位 160（`year=1950`、下游 0、各挂 4 个假前置）。**「基础→技术」依赖边 1982 条全部由 30 个基础节点承担**（占全库 7340 边的 27%），Top10 占 1909 条：数学 435 / 化学 421 / 经典力学 220 / 算法理论 215 / 工程学 169 / 分子生物学 94 / 材料科学 92 / 遗传学 89 / 免疫学 88 / 统计学 86。

**落地（形式 A：节点详情「理论与解释」区块；只加标注、不动现有边）**

| 层 | 内容 | 数量 |
|---|---|---|
| `nodes` 节点层 | `theory` 理论与解释 / `method` 方法·制度·工具 / `tech` 明确属技术却误挂「基础」 | 55（36/9/10） |
| `retro` 事后解释 | 技术先行、科学解释后至，含 useYear / explain.at / folk / note | 20 |

- 事后解释 20 条：冶金、陶器、发酵、水泥、玻璃、火药、指南针、机械钟、眼镜、望远镜、蒸汽机、疫苗、飞机、弓箭、水车、风车、拱、外科、轮、纺织。
- 「无科学解释期」由页面**实时计算**（`explain.at − useYear`）：冶金 ≈6660 年、发酵 ≈8860 年、火药 ≈810 年、蒸汽机 ≈112 年、疫苗 ≈94 年、望远镜 ≈58 年。
- 朴素解释标注（带起止年与被谁取代）：四元素说、燃素说（1667–1777 → 氧化说）、自然发生说（→ 微生物致病说）、瘴气说·体液说（→ 微生物致病说）、风水·天人感应（→ 地磁说 → 电磁学）、视觉射线说。
- 语料年份与实际起点不一致处已注明（如 `steam_engine` 语料记 1769，实际起点为纽科门机 1712）。

**实现**

- 新增 `assets/pages/theory_data.js`——**覆盖层**，只读标注，不改动任何 `dependsOn` / `enables` / 节点字段。
- `core.js` 新增 `buildTheoryBlock(t)`，挂入 `buildDetailHTML`（弹窗与整页详情共用单数据源），位置在「关系解说」之后；两层皆无标注的节点返回空串。
- `style.css` 新增 `.th-*` 样式（层徽章三色 / 三格指标 / 斜纹空白条 / 朴素解释 chips）。
- 16 个页面挂载 `theory_data.js`；`core.js` 与 `style.css` 版本串统一为 `20260912t`。

**范围与复核**：全库 2289 节点中 **74 个**会显示该区块；节点数、依赖边数与任何现有关系**均未变动**。干跑（`/tmp/run_theory.js`）校验：覆盖层 id 全部命中真实节点、`explain.by` 全部存在、无残留模板占位串；`check_docs` [OK]；模型页与纠错页干跑未受影响。

**纠错页登记**：本次审计以 `CR-2026-0912-theory-layer`（severity 中 / status 进行中）留档于「纠错总结」，记录三类问题——① 依赖网结构上无法表达「技术先用、理论后到」（`dependsOn` 硬编码前置 + `yearInv=0`）；② `basic` 分类混装 24 个非理论节点（仪器 / 技能·符号 / 材料 / 方法·制度 / 领域·未来项）；③ 160 个 `bx_*` 填充占位（year 全为 1950、下游 0、占 `basic` 的 53%）——并附带记录全库 summary 无「偶然 / 意外 / 巧合」、无朴素解释标注、淘汰侧不建模三项缺口。

**待办**（已同步登记进纠错页 `pendingQueue`）：

1. 本层为首版，年份按学界通行说法填写，**待逐条复核**（重点：`vaccination` 1890 / `airplane` 1904 / `compass` 1865 三条口径）。
2. `basic` 144 个真节点的人工重挂（含 24 个明显非理论者），其余 120 个亦待复核 theory / method / tech 归属。
3. 160 个 `bx_*` 填充占位清理（清理前须确认无真实节点引用它们）。
4. 依赖网引入 `rel="retro"` 事后解释边类型（允许理论年 > 技术年），并让 `yearInv` 检查按 `rel` 放行——这是让「事后解释」成为一等公民的前提；在此之前只能靠覆盖层标注。

---

## v0.9.18 · 修复 · 2026-09-12

### 构想 B 口径修正：前提齐备是「必要条件」而非「充分条件」

用户提出模型层的根本质疑：**技术常是多个技术共同导致；且技术条件成熟后，后置技术也不一定会诞生，可能由社会条件、偶然性等因素才催生。** 据此复核并修正模型页对「前提齐备律」的表述（仅改表述与方法日志，**不动数据与网络结构**）。

**被修正的过强断言**（原 `assets/pages/model.js` 第二节）：

> 实证锚点 · 有效滞后 Δ 极短（前提齐备即涌现）〔支持正反馈〕
> 近代真实技术的 Δ 中位仅 0–2 年——**前提一旦齐备，技术几乎立刻涌现**，正是「前提闭包被极快满足」的正反馈指纹。

**复核结论**：Δ 中位 0–2 年**可复现**（industrial 中位 1 / electrical 0 / info 2），但**解读错误**。主库实算（year≥1700 且有前置者 n=1879）：

- 滞后分布**双峰**：中位 6 年、但均值 **30.8** 年、p90 **100** 年、max **993** 年；Δ=0 占 35%，**Δ>10 年占 46%**，Δ≥100 年 **190 项**。
- 近零中位的两条成因都是**编年产物、非自然律**：① 前置集被**学科背景枢纽**主导（数学 648 / 化学 426 / 经典力学 289 个下游，早已就位，「齐备」近乎恒真，真正瓶颈被淹没）；② 编年惯例把技术年对齐到最晚前置年附近。
- 量化佐证：`year` 与 `max(前置年)` 的 Pearson **r = 0.727（r²≈0.53）**——前置只解释约**一半**的涌现时间方差。
- 文档口径不一：纪要 §13.2 记「中位滞后 37 年 / r=0.247」（基于早期 13392 节点快照；现主管线按 year≥1700 实算中位 6 年、均值 30.8 年、r=0.727，按其他年份截断则中位 6–7 年、r 0.67–0.93），同样与「几乎立刻涌现」抵触。即：该过强断言在文档内部早已被量级保守得多的旧口径否证，只是当时未被察觉。

**修正动作**：

- 卡片标题改为「前提齐备是『必要条件』，不是『充分条件』」，徽章 `pass` → `partial`（必要非充分）。
- 新增**四道门**流程：① 前置 AND 门 · **已建模** → ② 社会·需求门 · 未建模 → ③ 偶然之门 · 未建模 → ④ 路径·淘汰门 · 未建模 → 涌现。
- csub 改为**实时分位口径**（n / 中位 / 均值 / p90 / max / Δ=0 占比 / Δ>10 占比 / Δ≥100 项数全部实算，不写死）。
- 新增「口径声明（方法 v0.9 · 2026-09-12）」说明编造成因，并列举反例：火药 850 → 钻爆法 1843（**993 年**）、线膛 1490 → 后膛装填 1850（360 年）、化学 1661 → 石墨烯 2004（317 年）。
- 正反馈闭环图内「有效滞后 Δ ↓」标注为「（假说）」。
- `MODEL_METHOD` 方法日志追加 v0.9 条目。

**本轮甄别结论（留档，未改数据）**：

| 类 | 现象 | 实证 |
|---|---|---|
| Ⅰ | 多因共致（AND 门） | 主库前置数 `0: 6 / 1: 155 / ≥2: 2128`，**93% 为多前置**；但引擎一律 `max()` 当同质 AND 门，未区分必要/可选、瓶颈/背景，亦无权重。样例：智能割草机器人 10 前置、无人机 8、金属基复合材料 8、智能手机 7 |
| Ⅱ | 条件齐备却久候 | 46%（872 个）滞后 >10 年、190 项 ≥100 年；极端 993 年 |
| Ⅲ | 偶然性缺失 | 全库 summary 命中「偶然/意外/巧合」= **0 条**（火药←炼丹、青霉素←污染、X 射线/微波炉/特氟龙/糖精/硫化橡胶等均无标注） |
| Ⅳ | 分支淘汰 | 全库 summary 仅 4 条提「取代」（晶体管←真空管 / 内燃机←蒸汽机 / 触摸屏←键盘鼠标 / 无刷电机←机械电刷），均为**赢家视角**；网络只留生存者，**不记录淘汰侧**（失败支线、淘汰时点、失败原因） |
| Ⅴ | 占位年假象 | 仅全量集自动生成层：`year: 0` 占位（如 `筏板基础`）→ `桩筏基础 1950 ← 0` 假滞后 1950 年；**主库 `year===0` 为 0 个**，无此问题 |

**复核**：`node --check` 通过；模型页 dry-run RENDER OK、无残留 `${...}`、既有拟合值未变（P b 1.33/R² 0.874、K b 2.04/R² 0.932）、新字段渲染正确（n=1879 / 中位 6 / 均值 30.8 / p90 100 / max 993 / Δ=0 35% / Δ>10 46% / Δ≥100 190 / r=0.727）。数据与网络未动。

---

## v0.9.17 · 修复 · 2026-09-12

### enables 语义错误批量修正：95 个「灰色概念」占位清零（主库 + 全量镜像）+ 正式甄别表落档

对 enables 字段的系统性语义错误做批量修正，并顺带发现与修复全量镜像层同源残留。

**问题（用户复核抽查的五例）**：

- `trade`（交易）被列作 `sail`（帆船）/ `road`（道路）/ `horse_gear`（马具与马车）的使能物——三者是交通工具/方式，交易不会催生它们，方向反了。
- `social`（社会）被 `internet` / `www` / `smartphone` 使能——三条 summary 分别写「重塑人类社会结构」「大众信息空间」「服务全民化」，被自动生成批次按关键词机械提取成下游。
- `books`（书籍）被 `paper`（造纸）/ `printing`（印刷术）使能——实为造纸·印刷降低书籍成本、推动书籍普及，书籍是成果而非前因。
- `cooking`（烹饪）被 `fire`（用火）使能——烹饪只是火的用途之一，应属 applications。
- `hunting`（打猎）被 `bow_arrow`（弓箭）/ `firearm`（火器）使能——打猎催生弓箭较可能（需求拉动），但火器是火药催生的、火药是炼丹术催生的，与打猎无因果。

**全库扫描结论**：此类错误在 enables 字段共涉及 **95 个指向不存在 id 的断链目标**，被 `dump_data.js` 一并登记为 `kind=concept` 占位节点（graph.json 节点 2384 = 技术 2289 + 概念 95），前端 `core.js` 因 techMap 映射不到实体而以灰色虚框标签呈现，即「95 个灰色概念」。

**修正动作**：

- **主库 enables 清理**：`assets/data.js` 60 处 + `assets/techs_extra.js` 2 处，共移除 83 条指向灰色概念的断链引用；真实技术边（如 `sail→navigation`）保留不动。
- **graph.json 重导出**（`dump_data.js`）：概念占位 **95→11**、enables 边 304→206、断链 enables 12（= 11 个待实体化概念，`icbm` 被 `rocket` 与 `missile` 双处引用）；技术节点 2289 / dependency 7340 / 悬空 0 / 倒挂 0。
- **全量镜像同源残留修复**：定位到 `phase2_scores.json` 残留 **96 条 `(concept)` 占位评分条目**，经 `build_full_dataset.js`「评分独有节点」分支进入 `data_full.js`——这是该层灰节点长期存在的真正原因。在 build 脚本增加 `isConcept` 过滤（graph 节点 / new_techs / score 条目三处 + category 回填守卫）后重建：**TECHS_FULL 13413→13319、EDGES_FULL 17503→17394、概念节点 96→0**（移除的 94 项经逐 id 比对确认全为概念占位，无真实节点误伤、无新增）。
- **脏 id 清理**：`calendar(agri)→calendar_agri`（仍属待建真技术缺口）、`mars(未来)→mars_colony`（接上既有真实节点「火星殖民」）；全库含括号 id / 引用 = 0。
- **顺带修正 algorithm 错挂**：`gunpowder`（火药 850）、`optics`（光学 1000）的 dependsOn 移除 `algorithm`（算法理论 825）。全库 216 个 `algorithm` 下游中 213 个属信息/智能时代（AI/ML 合理），仅 3 个非 AI 时代，前两者已修；`mil_depthcharge` 属自动生成 stub 捏造依赖链，另案登记。
- **正式甄别表落档**：95 项灰色概念三分类（真技术物 33 / 抽象现象 56 / 应用·上下位 6）+ 逐条处置建议（**待实体化 11 / 去重 21 / 降为广义影响 63**）落为纠错页 `meta.enablesAudit`；纠错页新增可勾选渲染（`correction.js` + `style.css`），勾选状态本地记忆、可一键清除。
- **文档同步**：`README.md`（2 处）/ `tree_full.html` 全量节点 13,413→13,319、依赖/赋能边约 1.75 万→约 1.74 万；`check_docs` 门禁 OK。

**新发现登记（本轮未动）**：自动生成 stub 存在捏造依赖链——`mil_depthcharge`（深水炸弹 1910）dependsOn 竟含 `vacuum_cleaner`（真空吸尘器）/ `mat_ferrite` / `tr_simulator`，summary 亦写「建立在 真空吸尘器、调峰电站、铁氧体 之上」；`techs_more.js` 另约 209 条同构 dependsOn（`[rocket,algorithm,electronics]` 与 `[mathematics,computer,statistics,algorithm]` 两族）疑为模板注入，列入 pendingQueue 待专项甄别。

**复核**：主管线 2289 节点 / 7340 依赖边 / 悬空 0 / 倒挂 0 / 含括号 id 0；graph.json 与主库 0 差异；data_full 概念节点 0；纠错页 dry-run 95 行勾选表渲染正常；模型页 dry-run RENDER OK（P 1.33/R²≈0.874、K 2.04/R²≈0.932）；`check_docs` 门禁 OK。

**涉及文件**：`assets/data.js` · `assets/techs_extra.js` · `analysis-engine/data/graph.json` · `analysis-engine/build_full_dataset.js` · `assets/data_full.js` · `assets/pages/correction_data.js` · `assets/pages/correction.js` · `assets/style.css` · `README.md` · `tree_full.html`

---

## v0.9.16 · 修复 · 2026-09-10

### 全量管线镜像同步 + 无人机测绘主库漏修 + 史前/上古 12 处负年倒挂考古修正

把 v0.9.15 的主管线修正镜像到全量管线，补修主管线漏修的无人机测绘，并对 12 处含负年的年份倒挂做考古修正；顺带修复全量数据集建边的方向混写 bug。

**① 全量管线镜像（graph.json + data_full.js）**

- 先补修主库漏修项 `bld_dronesurvey`（无人机测绘）：当年 CR-2026-0909-dronesurvey 只改了 graph.json/data_full，主管线数组仍是旧错误态（year 2015、dependsOn 空、摘要含「二维材料/工业 4.0/摩擦纳米发电」模板）。本轮对齐 graph.json 正确版：year 2010、`dependsOn = [gps, computer, drone, camera, photography, uav_controller, multicopter]`、补 people/place、重写 summary/views/significance。
- 以 `dump_data.js` 从主管线重导出 graph.json：清除 654 条 graph-only 陈旧依赖边（含 v0.9.15 已清的约 622 条二阶/量子簇残留，另含 7 条 bld_dronesurvey 正确边已先行写回主库）、补入 110 条主库新增边（史前/上古真实节点），**graph.json 与主管线双向 0 差异**。
- 修复 `build_full_dataset.js` 的 dependency 方向混写：graph.json 的边存为「被依赖者→依赖者」，new_techs 边为「依赖者→被依赖者」，此前直接拷贝导致 EDGES_FULL 两种方向并存、全量科技树部分边渲染反向。现统一转置为「依赖者→被依赖者」。重建 data_full.js：**TECHS_FULL 13392→13413**（+21 主库独有史前/上古节点）、**EDGES_FULL 18048→17503**，反向重复对归零、污染枢纽（mat_2d/ene_wasteheat/mat_selfheal/ene_liquidair 等）被依赖数归零。
- 数据声明同步：README.md / tree_full.html 13,392→13,413 节点、约 1.8 万→约 1.75 万边。

**② 12 处负年倒挂考古修正（18 节点，负年 yearInv 12→0）**

- 年份/日期/纪元修正（附考古依据）：
  - `rope` -28000 → **-45000**（约 4.5 万年前；法国 Abri du Maras 尼安德特合股纤维绳直接证据 4.1–5.2 万年前）；
  - `woodworking` -50000 → **-300000**（约 30 万年前；德国 Schöningen 木矛为系统性木作最早实证）；
  - `hafting` -60000 → **-200000**（约 20 万年前；粘合/装柄复合工具直接证据可上溯 20 万年前）；
  - `arch` -100(classical) → **-1800(ancient)**（约 公元前 1800 年；美索不达米亚砖拱，早于罗马混凝土拱千年）；
  - `smithing` -3000 → **-1000**（铁器时代锻造普及口径，约 公元前 1200 年铁器兴起之后）；
  - `loom` -5000 → **-4000**（与纺织同步的织机阶段）；
  - `metallurgy`/`masonry` 维持约 公元前 5000 年（铜冶金/砌筑实证起点）；`aqueduct`/`bellows`/`charcoal`/`textile` 等相应保持。
- 7 条「后发专化使能/材料被当领域起源前置」硬边做**概念影响降级**（摘要注明，同 v0.9.15 哲学）：
  - `rope` 移出 `woodworking`/`hafting`/`fire_making`（装柄可用粘合剂与革筋、摩擦取火不需绳、木作不需绳）；
  - `mortar` 移出 `masonry`（早期干砌/泥浆砌筑先于石灰砂浆）；
  - `arch` 移出 `bridge`（木梁/石梁桥早于拱桥，拱桥为后起形式）；
  - `bellows`、`charcoal` 移出 `metallurgy`（铜冶金起源不需风箱/木炭，二者是青铜/铁高温冶炼阶段使能）；
  - `cement` 移出 `arch`（罗马火山灰水泥远晚于砖拱）。

**③ 其余登记**

- 52 项 era-vs-year「语义纪元」逐条复核：全部维持原判、不改数据（三类：上古延伸约 14 项、领域纪元如 electricity 1879=electrical / lasers 1960=electrical / plastics 1907=industrial、预测档位如 crispr/mrna_vaccine/reusable_rocket era=future 且 year=2025）。
- 新遗留登记（pendingQueue）：`ml_uav_1..126` 模板 cohort 同构依赖 rocket/algorithm/electronics（主库 + new_techs 双份），与二阶枢纽污染同源但属「衍生分支生成物」，待下轮批量甄别（本轮未动，不在 622 条镜像范围）。
- 模型页实时拟合值随年份/纪元修订微移（P 弹性 1.33/R²≈0.874、K 弹性 2.04/R²≈0.932；页面实时计算、无硬编码）。

## v0.9.15 · 修复 · 2026-09-09

### 网络年份/概念关系修正 + 二阶枢纽逐节点核验结案 + 量子两项研判

处理 v0.9.14 遗留的三类报告项（4 处年倒挂、二阶枢纽约 24 节点、量子节点定位），全部落地修正。

**① 4 处年倒挂 → 概念影响降级 + 年份/纪元修正**

- 硬边移除（降级为「概念影响」，摘要中注明；站内 `_upConcept` 承接科学解释类关系，对已建节点以「移除硬边 + 摘要注明」落实）：
  - `engineering` 去 `scientific_method`、`propulsion` 去 `rocket`、`combustion` 去 `thermodynamics`、`quantum_entanglement` 去 `quantum`。
- 年份修正：`engineering` 1500 → **-3000**（古代，与其下游建筑/桥梁谱系一致）；`propulsion` 1900 → **1960**（信息时代，先进推进工程化起点，其下游均为 2050+ 未来推进）。
- 纪元纠偏：`scientific_method` / `physics` / `chemistry` → earlymodern；`rocket` → electrical；`quantum_entanglement` → electrical（era-vs-year 失配 59 → 52，余项多为「语义纪元」合理保留或古早纪年噪声）。

**② 量子两项研判（web 检索佐证）**

- 量子纠缠（1935）**非「可能性强的理论」**：1980 年代起经 Bell 实验反复证实、2022 年获诺贝尔物理学奖；已作为量子密钥分发 / 量子隐形传态的**工程化资源**（QKD 进入实用化——京沪干线 / 城域量子保密网商用、墨子号星地 1200 km 量子密钥分发，2025 联合国定为国际量子科技年）。
- 量子科技（1980）**非「仅教育意义」**：对技术进步有**直接推动**（QKD 商用组网、量子计算「优越性」实验与云服务、量子传感用于心磁图 / 电网 / 管道探伤等）。
- 结论落地：两者均**保留为真实基础节点**；清除量子簇 3 条虚假下游（λ演算、计算机科学 ← 量子纠缠；蛋白质组学 ← 量子信息），重写 6 处模板化/重复摘要。

**③ 二阶枢纽逐节点核验结案（CR-2026-0909-hub2nd → 已修正）**

- 核验发现：24 个候选枢纽的下游是**整批「同纪元模板」注入**——每个枢纽的全部下游几乎都以该枢纽 year 为锚、同一年份横跨 build/info/manufact/military/transport 五类（例：余热回收 50 条下游全为 1800 工业纪元 cohort；液态空气储能全为 2010 智能纪元 cohort）。对照真实枢纽（半导体材料 → 晶体管/集成电路/激光器/卫星，异类异年）即可识别。
- 删除确认污染的 **590 条**下游引用（明细见纠错页 CR-2026-0909-hub2nd），保留真实下游：semiconductors 8、mat_cf 2（碳纤维复合材料/金属基复合材料）、electric_motor 8；其余 21 枢纽下游归零。

**复核**：主管线 edges 7938 → **7341**；yearInv 0（正年口径）；dupEdge 0；无自环 / 环 / 悬空 dependsOn；模型页 dry-run RENDER OK（P 弹性 1.33 / R²=0.875、K 弹性 2.03 / R²=0.933 实时渲染与独立复算一致）。
另：含负年全量口径另发现 12 处古早纪年噪声（如 `masonry(-5000)←mortar(-3000)`、`metallurgy(-5000)←charcoal(-4000)` 等 18 节点），需考古口径逐例定夺，已列入待核清单未擅改。

**涉及文件**：`assets/techs_extra.js`、`assets/techs_extend.js`、`assets/data.js`、`assets/pages/correction_data.js`

---

## v0.9.14 · 修复 · 2026-09-09

### 主管线网络审计修正 + 模型页 Phase3 待硬件暂存

对模型页 / 分析页所用的主管线技术网络（2289 节点依赖 DAG）做**全量遍历审计**，并在数组源头做镜像修正。

**网络遍历审计结论**
- 结构健康项：2289 节点无重复 id、无自环、无环、无悬空 dependsOn（概念 enables 111 条为设计保留）。
- 残留问题（一阶枢纽误接 + 重复依赖 + 年倒挂）：
  - 一阶枢纽残留（v0.9.11 只清了全量管线，主管线源头未清）：`mat_2d` 下游 57 / `ene_tribo` 21 / `mat_selfheal` 22 / `mfg_industry40` 5。
  - 同节点重复依赖 86 条（`solar`、`genetics` 等在 dependsOn 被列两次，污染下游统计）。
  - 年倒挂 4 处（`engineering←scientific_method`、`propulsion←rocket`、`combustion←thermodynamics`、`quantum_entanglement←quantum`）——判定为年代标注噪声，保留待人工定夺。

**修正动作（写回数组源头）**
- `techs_extra.js`：78 个条目去污染 / 去重，移除 101 条枢纽下游引用 + 1 处重复。
- `techs_more.js`：85 个条目去重（solar / genetics 等）。
- `data.js`：2 个 AI / 大模型基础条目移除 `mat_2d`、`ene_tribo` 依赖（4 条误接边）。
- 复核：主管线边 **8129 → 7938**（−191）；四枢纽下游 = 0；dupEdge = 0；仍无自环 / 环 / 悬空。
- 保留挂起：二阶枢纽约 24 节点（`ene_wasteheat` 50 / `ene_liquidair` 31 / `ene_borehole` 25 / `ene_salinity` 25 / `ene_dishstirling` 25 / `mfg_grindingwheel` 15 等）维持「待人工核验」（关联纠错 CR-2026-0909-hub2nd）。

**数学模型页：Phase 3 待硬件暂存**
- 当前环境无 GPU / TPU，进阶模型脚本执行暂停。第九节新增「⚠ Phase 3 待硬件」提醒框 + 折叠详情：M1 node2vec / M2 PyG-GCN / M4 PyG-GAEn 最小可运行脚本方案、算力成本量级设计估算（云牌价，实测后回填）、速查表将补「数据需求量」列、验收协议（与本页实时统计交叉核对）。
- MODEL_METHOD 方法日志加 v0.8。

**涉及文件**：`assets/techs_extra.js`、`assets/techs_more.js`、`assets/data.js`、`assets/pages/correction_data.js`、`assets/pages/model.js`

---

## v0.9.13 · 功能 · 2026-09-09

### 新增「技术演化的数学模型」页（5 构想 + 十种进阶模型 + 验证硬化）

把项目技术依赖网形式化为可计算、可验证、随研发迭代演进的数学对象，新建独立数学模型页。

**5 个构想（A–E）**
- **A 社会-环境涌现**：参数扩充至约 20 个（新增人力资本 L/ℓ/K、社会结构 Γ/C/u、定向创新 r、资源环境 R/Θ_env），核心涌现速率方程 λ(t)=κ·[P·(ρ/ρ₀)^γ]·φ(clim)·ψ(E,R)·[1+G/G_ref]·Ξ(t) 保留用户给定形式，新增扩展因子 Ξ(t) 吸收新参数；显式点出正反馈闭环（信息流加速→重组加速→滞后缩短→λ 再加速），并以「按时期滞后中位 Δ」（近代真实技术 Δ 中位 0–2 年）作实证指纹。
- **B 前提闭包+滞后**：涌现年 = 最晚前置年 + 经验滞后分布 L̂；year≥1700 实时拟合（n≈2040，中位 10 年、均值 66.5 年），给出分位区间估计。
- **C 组合涌现**：分类对共生指数 ℒ（9×9 热力图），本语料约 80% 技术属汇聚诞生（≥3 前置且跨≥2 类，页面实时计算）。
- **D 重要性=中心性**：综合重要性 x_i=0.40·后代+0.18·入度+0.27·PR+0.15·中介；各时期中位递减（古典峰值 ~0.24 → 信息/智能时代 <0.03）。
- **E 物质能量信息三元**：按主类归入 M/E/I 三元；信息流速度 v_i 量化为「各时期信息类技术涌现速率（项/百年）」，电气/信息/智能时代呈数量级跃升。

**十种进阶模型（第九节，隐性参数 · 神经网络 · 专用硬件）**
- M1 潜空间嵌入 / M2 GCN 分类 / M3 VAE 表征 / M4 图自编码器链路预测 / M5 时空图网络 / M6 Transformer 序列 / M7 图扩散生成 / M8 IBP 贝叶斯非参数 / M9 PINN 物理约束 / M10 NOTEARS 潜在因果发现。
- 标注隐性参数（嵌入矩阵 Z、隐向量 μ/σ、注意力 W_QKV、无限维特征矩阵、潜在混杂变量等）、所需硬件（CPU/GPU/TPU 集群）与后续实现库（PyG/DGL/HuggingFace/DiGress/Pyro·NumPyro/gcastle），并说明其作为构想 A–E 的「计算化身」衔接关系。
- 打磨：每个模型补「代价/局限」；新增「模型选用速查表」（任务→推荐模型）。

**Phase 1 验证硬化**
- 构想 B 样本外验证：留最近 20% 技术作测试集，以训练集滞后中位作预测，对照真实年得 MAE/RMSE，证明「前提闭包+滞后」在语料外仍成立。
- 构想 A 量化正反馈指纹：信息流速率↔总体涌现速率 Pearson 相关 r，把正反馈闭环落到可计算数值。
- 验证总览表新增「证据强度」列（强/中 + 样本量），验证严格度分级。
- 设立 MODEL_METHOD 方法日志（v0.1–v0.6），让建模方法随研发迭代而演进、可追溯。

**配套门禁**
- 新建 tools/check_docs.js：运行时算权威值 TOTAL=2289 / TOTAL_FULL=13392，扫描正文写死数字；挂 .git/hooks/pre-commit 自动拦截过期数字（仅 ERROR 阻断）。

**涉及文件**：`model.html`、`assets/pages/model.js`、`assets/shell.js`、`assets/style.css`、`tools/check_docs.js`、`.git/hooks/pre-commit`

---

## v0.9.12 · 文档 · 2026-09-09

### 数据口径澄清：Tier B 1915 gap 与预览工具注入属性

**Tier B 1915 gap 口径**
- 定义：`tierB_gap.json` 的 1915 条 = `year≥1750` 且 `people` 与 `place` 同时缺失的节点，需补「发明者/地点」（Tier B = web 研究回填）。
- 9 类分布：life296 / info245 / basic249 / build216 / transport209 / military202 / energy196 / material188 / manufact114 ＝ 1915。
- 关键提醒：其中大量是程序化生成的「学科/子领域」节点（如 `bx_math_*`、`it_alg_*`、`ml_uav_*`），本就不应有单一发明人 → **按设计留空**。所以「1915 gap」不是「都要填」，而是「1915 个候选，其中只有地标性发明/理论值得高精度回填」。
- 进度：已首批回填 29 个地标节点（铅笔、青霉素、X 射线、ENIAC、万维网、高铁、微处理器…），gap 由 1944→1915，剩余 1915 为 backlog。

**预览工具注入属性非问题（记录教训）**
- 内置预览/自动化工具会在 HTML 标签上注入 `data-page-node-id="..."` 追踪属性，属**工具噪声、非用户内容、非数据问题**。
- 此前一度被当作「未提交改动」处理，实为误报；今后**勿据此提交或修改文件**，直接还原即可。

**涉及文件**：`assets/pages/correction_data.js`、`版本迭代日志.md`

---

## v0.9.11 · 修复 · 2026-09-09

### 依赖关系纠错（续）：清理枢纽节点误接下游与悬空边

延续无人机测绘案例做全库自检：删除二维材料 / 工业 4.0 / 摩擦纳米发电 / 自修复材料 4 个枢纽节点的全部 104 条误接下游边，并清理 21 条指向不存在节点的悬空边；保留石墨烯→二维材料这一条语义正确的边。

**科技树变更原因**
- **影响范围**：依赖网——删除 4 枢纽节点 104 条误接下游边 + 21 条悬空边，`data_full.js` 同步重建。
- **原因**：Phase 3 链接预测 / catalog 合并阶段对通用技术名做了过度泛化的前因推断，把二维材料等当作万能前置注入一批共享模板下游（transport/build/info/military），导致来龙去脉页大量无关上游；另有部分边引用了合并后已消失的节点 id。无人机测绘案例是这一系统性偏差的可见症状。
- **具体动作**：`mat_2d`(56) / `mfg_industry40`(4) / `ene_tribo`(20) / `mat_selfheal`(24) 下游全清；保留 `mat_graphene→mat_2d`（石墨烯催生二维材料研究，语义正确）。全库边数 8397→8275。

**变更要点**
- `graph.json`：删除 `mat_2d` 全部 56 条、`mfg_industry40` 全部 4 条、`ene_tribo` 全部 20 条、`mat_selfheal` 全部 24 条下游误接边，合计 104 条。
- `graph.json`：删除 21 条悬空依赖边（18 条 target 缺失 + 3 条 source 缺失）。
- `graph.json`：保留 `mat_graphene→mat_2d`（核验正确）。
- 运行 `build_full_dataset.js` 重建 `assets/data_full.js`，全库边数 8397→8275。

**涉及文件**：`analysis-engine/data/graph.json`、`assets/data_full.js`、`assets/pages/correction_data.js`

---

## v0.9.10 · 修复 · 2026-09-09

### 依赖关系纠错：修正「无人机测绘」前因并新增「纠错总结」页

审计发现「无人机测绘」的直接上游被误设为二维材料 / 工业 4.0 / 摩擦纳米发电，修正为 GPS / 计算机 / 无人机 / 相机 / 摄影 / 飞控 / 多旋翼；同步新增「纠错总结」页专录此类质量事件。

**科技树变更原因**
- **影响范围**：依赖网——修正 `bld_dronesurvey` 入边（删 3 条错误 + 加 7 条正确），`data_full.js` 同步。
- **原因**：原 `graph.json` 中 `bld_dronesurvey` 的入边是 catalog 合并环节产生的错误因果（二维材料 / 工业 4.0 / 摩擦纳米发电均非其前置技术），导致来龙去脉页生成「这些技术汇聚使其商品化」的荒谬文案，并回溯出 65 项含大量无关节点的上游列表。修正直接前因才能使依赖网可信。
- **具体动作**：`year` 2015→2010 与 catalog 对齐；重写 summary、补 people/place；`app.js` 代际标签由「第 N 代（更早）」改为「直接上游 / 第 N 代上游」。

**变更要点**
- `graph.json`：`bld_dronesurvey` 入边由 `{mat_2d, mfg_industry40, ene_tribo}` 修正为 `{gps, computer, drone, camera, photography, uav_controller, multicopter}`。
- `graph.json`：`year` 2015→2010；重写 summary；补 people/place（DJI / 开源飞控 / Pix4D 等）。
- `assets/app.js`：`renderLineageSide` 代际标签改为「直接上游 / 第 N 代上游」；顶部 prose 改为克制因果表述。
- 新增 `correction.html` + `assets/pages/correction_data.js` + `correction.js`：专录依赖关系 / 前因后果 / 归属类纠错事件，并内置全库自检方法论与待检清单。
- `shell.js` 导航新增「纠错总结」入口；`style.css` 补 `.cr-*` 样式。
- 运行 `build_full_dataset.js` 重建 `assets/data_full.js`，`bld_dronesurvey` 上游已更新。

**涉及文件**：`analysis-engine/data/graph.json`、`assets/app.js`、`correction.html`、`assets/pages/correction_data.js`、`assets/pages/correction.js`、`assets/shell.js`、`assets/style.css`、`assets/data_full.js`

---

## v0.9.9 · 修复 · 2026-09-08

### Phase 3 修正 · 实证发现依赖边方向读反，全量回滚并按权威方向重写回

审计揭示 `graph.json` 依赖边权威方向为 **source(前提)→target(依赖者)**，而 v0.9.8 引擎按相反假设运行，2883 条写回边方向错乱（如「疫苗→骨科」被写成疫苗是骨科前提）。回滚全部 2883 条错误边，修复引擎方向与后代计数 bug，升级护栏，按权威方向重写回 25 条高质量近因前提边，并新增 hold-out 科学评估。

**科技树变更原因**
- **影响范围**：依赖边方向体系——回滚 2883 条方向错乱边 → 写回 25 条方向正确的近因前提边（依赖边 17862，`data_full.js` 同步）。
- **原因**：v0.9.8 把依赖边 (source,target) 误读为 (依赖者,前提)，实际权威方向相反。**铁证**：`boiler(1765)→steam_engine(1769)` 只存在该方向；且全部依赖边约 80% 满足 `source.year < target.year`，与「前提早于依赖者」一致，反向假设下该比例仅 20%、语义崩塌。方向反了会使整张反图上的支撑度计算与写回全部错乱，必须回滚纠正。
- **具体动作**：① `load_full` 边方向改为 (t,s) 统一为 (依赖者,前提)；② 后代计数由树式累加改为去重闭包 bitmask（稠密 DAG 原式指数爆炸，desc_ratio 一度达 1e22）；③ 写回方向交换为 (source=前提→target=依赖者) 并加年份门禁；④ 四重护栏：frac=1.0 + 支撑≥2 + 同域/basic + 近因窗口 gap≤3 + 普适度 desc<0.5。写回 25 条全部通过时间箭头校验（0 违反）。

**变更要点**
- **回滚**：删除 `graph.json` 中 2883 条 `provenance=phase3_linkpredict` 错误边；备份 `analysis-engine/data/backup/graph.json.pre-phase3-20260908.bak`（可审计可回退）。
- **修复**：`phase3_linkpredict.py` 方向 bug（load 读反 + 写回方向反）与 desc 重复计数指数爆炸 bug。
- **护栏升级**：重推后候选 1237 → 可写回 25 条（语言/科学方法等普适远祖不再混入）。
- **新增 hold-out 评估** `analysis-engine/phase3_eval.py`（seed=42，留出 20% 规则可覆盖现存边 796 条）：AUC=0.564（随机 0.5），Recall@1000=0.49 vs 随机 0.27（约 1.8×）。结论：规则信号中等，写回质量由护栏保证 precision。结果存 `data/phase3_eval.json`。
- **重写回 25 条**高可信近因边：电力系统→配电自动化/数字化变电站/微机继电保护、集成电路→异构计算/可编程片上系统、火药→无后坐力炮/佛郎机铳、正态分布→假设检验、米制→国际单位制、人造卫星→星载定位接收、火炮→舰炮舷侧齐射等。
- 重跑 `build_full_dataset.js` 刷新 `data_full.js`（dependency 17837→17862）；重跑 `forecast_engine.js` 预测分档不变（L2=105 / L3=60 / L5=10）。

**涉及文件**：`analysis-engine/phase3_linkpredict.py`、`analysis-engine/phase3_eval.py`、`analysis-engine/data/phase3_eval.json`、`analysis-engine/data/graph.json`、`analysis-engine/data/backup/graph.json.pre-phase3-20260908.bak`、`analysis-engine/data/phase3_results.json`、`analysis-engine/data/phase3_inferred_edges.json`、`analysis-engine/data/phase3_report.md`、`assets/data_full.js`

---

## v0.9.8 · 功能 · 2026-09-08

> ⚠ **修正声明（v0.9.9）**：本版本写回方向基于错误的依赖边方向假设，2883 条边已全部回滚；正确结果见 v0.9.9（25 条方向验证过的近因边）。以下为当时的原始记录，保留用于审计。

### Phase 3 · 链接预测补全隐含技术前置关系

实现确定性、可解释的链接预测引擎，基于依赖网祖先闭包计算「前提支撑度」，补全「长度=2」的隐含前提缺口；写回 2883 条高可信隐式依赖边，使科技树前置关系更完整。

**科技树变更原因**
- **影响范围**：全量依赖网——17837 → 20720 直接依赖边（+2883，+16.2%）。
- **原因**：此前依赖网稀疏（平均约 1.35 条直接前提），大量「A 的已知前提都直接依赖 B」的隐含前提关系未显式记录。用前提支撑度（B 为 A 多数直接前提的直接前提 ⇒ 补 A→B）补全，使科技树的前置关系更完整、下游中心性与未来涌现推演更准。
- **具体动作**：护栏 frac=1.0 且 支撑≥2 且 同域或属基础科学；边打 `provenance=phase3_linkpredict` 标记，可审计/可回退。候选共 7886，写回 2883。

**变更要点**
- 新增 `analysis-engine/phase3_linkpredict.py`：bitmask 祖先闭包 + 前提支撑度链接预测（确定性、非黑箱 ML），输出 `phase3_results.json` / `phase3_inferred_edges.json` / `phase3_report.md`。
- 补全方法：仅补「长度=2 缺口」（A 的直接前提 p 直接依赖 B ⇒ A→B 隐含），避免把多跳传递依赖误当新边；年份顺序由 DAG 拓扑自动保证。
- 写回 2883 条高可信隐式前提边至 `graph.json`（provenance 标记）；重跑 `build_full_dataset.js` 刷新 `data_full.js`（边 21024）。
- 依赖网经校验仍为 DAG（无环）；重跑 `forecast_engine.js` 保持未来涌现预测一致（L2=105 / L3=60 / L5=10 不变）。

**涉及文件**：`analysis-engine/phase3_linkpredict.py`、`analysis-engine/data/phase3_results.json`、`analysis-engine/data/phase3_inferred_edges.json`、`analysis-engine/data/phase3_report.md`、`analysis-engine/data/graph.json`、`assets/data_full.js`

---

## v0.9.7 · 功能 · 2026-09-08

### 新增「科技树变更原因」独立栏目

按用户要求，在版本迭代日志新增独立栏目，专门记录每次改变整张科技树（结构 / 规模 / 评分体系 / 推演框架）的原因，并回填历史原因，便于回溯「树为何变成现在的样子」。

**变更要点**
- 数据层：changelog 条目新增可选字段 `treeChange{ scope 影响范围, reason 变更原因(核心), detail 具体动作 }`；任何实质改动科技树的版本都须填写原因。
- 渲染层：`renderChangelog()` 新增「科技树变更原因 · 全程追踪」表（列：版本 / 日期 / 变更 / 影响范围 / 变更原因），由 `treeChange` 自动汇总；统计区新增「科技树变更」计数。
- 卡片层：每条目录卡片内嵌高亮块，展示该版本的科技树影响范围与变更原因。
- 回填 v0.1.0 / v0.3.0 / v0.8.0 / v0.9.2 / v0.9.3 / v0.9.4 共 6 个版本的科技树变更原因。
- 同步《版本迭代日志.md》：新增追踪表 + 各版本原因块；`style.css` 补 `.cl-ttrack*` / `.cl-tree*` 样式。

**涉及文件**：`assets/pages/changelog_data.js`、`assets/app.js`、`assets/style.css`、`版本迭代日志.md`

---

## v0.9.6 · 功能 · 2026-09-08

### 新增独立「文献谱系」页（历史相关研究，不进入技术网络）

按用户要求，把历史相关研究单独做成可点开查看详细介绍的页面；这些内容属方法论谱系而非技术，明确不进入 13392 节点技术演化网络。

**变更要点**
- 新增 `research.html` + `assets/pages/research.js` + `assets/pages/research_data.js`：8 张研究卡（TRIZ / 技术预测学 Delphi / 复杂网络引文分析 / 卡尔达肖夫-德雷克 / Arthur 组合进化 / 内史外史 / SNA / Polak&Arthur 计算实验），点击弹窗显示详细介绍（导语 + 正文 + 工具对应表 + 本项目借鉴 + 参考文献）。
- 页面顶部明确标注：文献谱系是「研究范式」而非技术，不进入技术演化网络。
- 复用站点 `#modal` 弹窗与全局搜索（本页做卡实时筛选）；`shell.js` 导航新增「文献谱系」入口。
- `style.css` 补 `.research-*` / `.r-*` 系列样式。

**涉及文件**：`research.html`、`assets/pages/research_data.js`、`assets/pages/research.js`、`assets/shell.js`、`assets/style.css`

---

## v0.9.5 · 文档 · 2026-09-08

### 奠基方法论对话完整存档（§3.0 扩充）

将项目启动时的关键方法论对话无遗漏地补入《项目构建研究思路与全程纪要》§3.0，并标注其旧框架表述与后续 §1/§13 校正的关系。

**变更要点**
- §3.0 由 4 条检索摘要扩写为完整存档：含 DNN 规模对照表（2265 节点/600 边 vs DL 甜区）、TRIZ/Delphi/复杂网络/卡尔达肖夫-德雷克四类文献谱系及项目对应表、阶段 0–3 四阶段算法方案表、「叙述层 vs 推理层」关键提醒、当时建议的四项下一步。
- 补「事后印证」段，将奠基方案与后续 Phase 1–5 落地一一对应（阶段 0=graph.json、阶段 1=graph_engine.py、阶段 2=phase2_score.py、阶段 3=链接预测、§13 推演引擎）。
- 加语境说明：对话中的「科幻科技」按校正后目的理解为「严肃科技方向的未来可行性」；方法论内核（确定性优先、可解释、链接预测为唯一可选 ML）被全程沿用且实证有效。

**涉及文件**：`项目构建研究思路与全程纪要.md`、`版本迭代日志.md`、`assets/pages/changelog_data.js`

---

## v0.9.4 · 功能 · 2026-09-08

### Phase 5 · 未来涌现推演引擎（项目真正核心落地）

按校正后目的，构建确定性推演引擎：从真实网络实算「前提齐备律」滞后分布，对每个严肃科技方向按前提闭包推导 L2–L5 与涌现时期区间，全程禁用手工年份。

**科技树变更原因**
- **影响范围**：推演层——以确定性引擎替代手工年份，新增 L2/L3/L5 涌现推导。
- **原因**：为落实校正后目的（严肃科学、非科幻），不能用臆测年份。从真实网络实算「前提齐备律」滞后分布（year≥1700, n=6684, p50=26/p90=147），按前提闭包推导涌现时期，使科技树的「未来枝」由内生规律产生而非外部假设。
- **具体动作**：L2=105（中位≈2052）/ L3=60（中位≈2078）/ L5=10（物理制约）；彻底替代 extend_net.js 手拍年份。

**变更要点**
- 新增 `analysis-engine/forecast_engine.js`：载入全量网络，计算 year≥1700 的滞后分布（n=6684，p50=26 / p90=147 年）。
- 推导分档：L2=105（前提齐备，涌现中位≈2052，如通用人工智能/量子计算机/自动驾驶）、L3=60（L2 跨域组合级联生成，中位≈2078）、L5=10（物理制约：曲速引擎/虫洞/超光速通讯等）。
- 输出 `analysis-engine/data/forecast_results.json`；《项目构建研究思路与全程纪要》§13.3 标注为已落地并补实测数据。
- 彻底替代 `extend_net.js` 的手工臆测年份，符合校正后「严肃科学、非科幻」目的。

**涉及文件**：`analysis-engine/forecast_engine.js`、`analysis-engine/data/forecast_results.json`、`项目构建研究思路与全程纪要.md`

---

## v0.9.3 · 重构 · 2026-09-08

### 项目目的校正：从「科幻可行性」转向「真实技术拆解 + 规律 + 未来涌现推演」

用户明确指出项目目的被误读为「研究科幻作品科技如何实现」。校正为：拆解真实人类技术发展、结合时代背景与技术前提建网络、找出联系与规律、推演未来何时/何条件涌现何技术（严肃科学，非科幻）。

**科技树变更原因**
- **影响范围**：数据净化——隔离 extend_net.js 手工拍年份的 117+ 未来节点（2050–2199 仅 51 个）。
- **原因**：用户校正目的为「严肃科学、非科幻」。手工臆测的科幻外推年份节点污染了推演正确性，属不该进入真实技术演化网络的数据，故明令排除；未来档此后一律由网络前提闭包 + 前提齐备律推导，不再手拍。
- **具体动作**：五分法重新定位为「技术涌现可行性五档」（输出而非科幻外推）；项目更名。

**变更要点**
- 重命名项目为「人类技术演化网络 · 规律挖掘与未来涌现推演」，废弃「科幻科技实现蓝图（现实锚点层）」表述。
- 《项目构建研究思路与全程纪要》§1 标注旧框架为误读、写入校正后权威目的；§13 新增「规律库（6 条已实跑验证的规律）+ 未来涌现推演引擎方法论」。
- 隔离污染：`extend_net.js` 手工拍年份的 117+ 未来节点、2050–2199 仅 51 节点的臆测数据不得进入推演引擎；未来档须由网络前提闭包 + 规律推导。
- 五分法重新定位为「技术涌现可行性五档」（已实现/目前可实现/未来可能/未来较难/受物理制约），输出而非科幻外推。

**涉及文件**：`项目构建研究思路与全程纪要.md`、`版本迭代日志.md`、`assets/pages/changelog_data.js`

---

## v0.9.2 · 功能 · 2026-09-06

### Phase 4 · 全量科技树站点集成（数据层 + 可视化）

打通分析引擎到站点的数据管道，新增「全量科技树」页，可视化 13392 节点与第一性原理五维评分。

**科技树变更原因**
- **影响范围**：全量科技树——13392 节点 + 18141 边 接入站点可视化层。
- **原因**：此前评分与网络只存在于 analysis-engine，站点看不到。build_full_dataset.js 合并 graph.json(原库富字段)+new_techs(11031)+phase2_scores(13392)，重建依赖/赋能边，使评分卡与网络在站点层可交互，是「树」首次完整呈现给用户。
- **具体动作**：assets/data_full.js（8.1MB，summary 覆盖率 100%）；D3 力导向渲染 + 按分类/tier/综合分着色筛选。

**变更要点**
- 新增 `analysis-engine/build_full_dataset.js`：合并 graph.json(原库富字段) + new_techs.json(11031) + phase2_scores.json(13392 评分)，重建依赖/赋能边，输出 `assets/data_full.js`（全量评分、summary 覆盖率 100%）。
- 新增 `tree_full.html` + `assets/pages/tree_full.js`：加载全量数据集，D3 力导向渲染，支持按分类 / 成熟度(tier) / 综合分着色与筛选、全球搜索高亮。
- 详情弹窗含第一性原理五维评分卡（D1–D5）+ 综合分 + 上下游依赖链。
- `shell.js` 导航新增「全量科技树」入口；`changelog_data.js` 记 v0.9.2。

**涉及文件**：`analysis-engine/build_full_dataset.js`、`assets/data_full.js`、`tree_full.html`、`assets/pages/tree_full.js`、`assets/shell.js`、`assets/style.css`

---

## v0.9.1 · 文档 · 2026-09-06

### 补入历史相关研究并审计文档缺口

在《构建研究思路与全程纪要》新增 §3 历史相关研究（文献溯源 + 实证结论），并补 §12 文档缺口审计。

**变更要点**
- 新增 §3「历史相关研究：溯源与实证」：3.1 文献与方法溯源（Arthur 2009 组合进化、Basalla 1988 进化树、内史/外史、技术网络 SNA）；3.2 本项目实证历史研究结论（最长链 211 跳、割点 1859、拓扑最深=现代医学、组合诞生、五维评分→五分法映射）。
- 全文章节顺延重编号（原 §3–§10 → §4–§11），交叉引用同步修正。
- 新增 §12「文档缺口与待补」审计，列出 10 项待补内容。
- 新增附录 C 关键外部文献，便于对外引用。

**涉及文件**：`项目构建研究思路与全程纪要.md`

---

## v0.9.0 · 功能 · 2026-09-06

### 新增「版本迭代」变更日志页（本页）

建立项目逐日 / 逐次变更档案，集成至全站导航，并持续记录后续所有迭代。

**变更要点**
- 新增 `changelog.html` 页面 + `assets/pages/changelog.js` 渲染器 + `assets/pages/changelog_data.js` 数据。
- 在 `shell.js` 主导航新增「版本迭代」入口；首页快速导航同步补充。
- 采用数据驱动的变更条目结构（date / version / type / title / summary / changes / files），便于后续每次变更直接追加一条。
- 回填 v0.1.0–v0.8.0 关键里程碑（据项目文件时间戳与开发记录重建），自此起逐条如实登记。
- 注：v1.0.0 预留给「站点集成（Phase 4）」完成时发布。

**涉及文件**：`changelog.html`、`assets/pages/changelog.js`、`assets/pages/changelog_data.js`、`assets/shell.js`、`assets/style.css`、`index.html`

---

## v0.8.0 · 里程碑 · 2026-09-04

### 分析引擎 Phase 1/2：图算法 + 第一性原理评分

引入确定性 Python 分析引擎，完成依赖图门禁、图算法度量与第一性原理五维评分。

**科技树变更原因**
- **影响范围**：评分体系——13392 节点首次获得五维评分与 L1–L5 五档标记。
- **原因**：此前科技树只有「节点 + 依赖边」，无法量化成熟度。引入第一性原理五维（D1 物理可行 / D2 时间成熟 / D3 依赖深度 / D4 系统织密 / D5 跨域辐射）后，树从「有无依赖」升级为「成熟度可量化、可推演未来档」。
- **具体动作**：L1 已实现 13168 / L1-基石 4 / L1-前沿 103 / L2 59 / L3 27 / L4 19 / L5 12；门禁全绿（断边 0 / 年份倒置 0 / DAG 无环）。

**变更要点**
- `validate_final.py` 最终质量门禁全绿：依赖边 17923、断边 0、同年依赖 0、年份倒置 0、DAG 无环、2927 弱连通分量。
- `graph_engine.py`（Phase 1）：PageRank / HITS / 拓扑层级 / 社区发现 / 割点 / 最长关键路径 → 最长演化链 211 跳（粉彩瓷→…→陶器→红铜→青铜）；割点 1859（化学 848 / 数学 784 / 电子计算机 643）。
- `phase2_score.py`（Phase 2）：第一性原理五维评分（D1 物理可行 / D2 时间成熟 / D3 依赖深度 / D4 系统织密 / D5 跨域辐射）→ L1 已实现 13168 / L1-基石 4 / L1-前沿 103 / L2 59 / L3 27 / L4 19 / L5 12。
- 工具链决策落地：LLM 仅用于知识生成，图算法 / 统计 / 校验一律用确定性 Python（networkx），CPU 秒级，无需 GPU / 外部 API。
- 修正若干算法缺陷：同年限规则、D5 跨域辐射方向（predecessors 下游）、阈值分离（成熟 vs 前沿）。

**涉及文件**：`analysis-engine/validate_final.py`、`analysis-engine/graph_engine.py`、`analysis-engine/phase2_score.py`、`analysis-engine/data/phase1_report.md`、`analysis-engine/data/phase2_report.md`

---

## v0.3.0 · 数据 · 2026-09-02

### 88 任务域骨架与子分类生成流水线

采用 88 任务域骨架，建立「任务域 → 子分类」生成流水线与统一标签体系。

**科技树变更原因**
- **影响范围**：全量网络规模——2362 → 13392 节点（+11031）。
- **原因**：此前树仅覆盖原始库，缺失大量现代技术域。改用 88 任务域骨架 + 子分类生成流水线，按 DAG 年份排序规则批量扩充，使网络从「骨架」变为「近全量人类技术覆盖」。
- **具体动作**：存量打标 448、跳过 739（重复/无法归属）；目标约 20000 节点，本版未达标，留待 Tier B 内容补全。

**变更要点**
- 在 `analysis-engine/catalogs/` 落地 88 个任务域 catalog（cat/sub/families[].items[] 结构），含 `_SCHEM.md` 规范、`_ANCHORS.json`（110 锚点）、`_filemap.json`（88→文件名）。
- 编写 `gen_subcat.js` 编译器：依 DAG 年份排序规则生成子分类条目并打统一 subcategory 标签。
- 全库新增 11031 条技术条目，存量打标 448、跳过 739，全库达 **13392 节点**（目标约 20000）。
- 打包 `tech-evolution-project.zip` 作为阶段性成果快照。

**涉及文件**：`analysis-engine/catalogs/`、`analysis-engine/gen_subcat.js`、`analysis-engine/data/subcat/new_techs.json`

---

## v0.2.0 · 功能 · 2026-08-28

### 全页面体系与关系网分析页

补全 12 个站点页面，建立统一导航、全局搜索与详情弹窗，上线关系网分析。

**变更要点**
- 新增 worldview / browse / timeline / tree / lineage / method / midtech / progress / sop / analysis / detail / index 共 12 个页面。
- 由 `shell.js` 统一注入 header 导航（含当前页高亮）、全局检索框、详情弹窗与页脚计数。
- 上线 `analysis.html`「关系网分析」：基于依赖网计算 PageRank / 中介中心性 / 组合诞生推演。
- 确立《研发 SOP》与「方法论」页，作为内容质量门禁与升级标准。
- 按《研发 SOP》将 993 条模板占位升级为非模板简介，清除套话与中英混排。

**涉及文件**：`*.html`、`assets/shell.js`、`assets/app.js`、`assets/pages/analysis.js`、`assets/pages/method.js`、`assets/pages/sop.js`

---

## v0.1.0 · 里程碑 · 2026-08-26

### 静态站点骨架 v1.0

搭建「技术演化数据库」静态多页站点（MPA）骨架，确立档案/蓝图卷宗视觉体系。

**科技树变更原因**
- **影响范围**：原始技术库——≈2265 条技术 → 依赖图 2362 节点。
- **原因**：项目启动首次建网：确立 9 大分类与历史分期体系，搭出 dependency 依赖边骨架。此后所有新增节点/边均以此为基础扩展，是整个科技树的「根版本」。
- **具体动作**：配套 tools/ 工具集（gen_techs / disassemble / relink_parents / extend_net 等）作为建网流水线；边 type=dependency。

**变更要点**
- 确立 9 大分类（科学基础 / 材料 / 能源 / 制造 / 交通 / 信息 / 生命 / 建筑 / 军事）与历史分期体系。
- 收录约 2265 条原始技术，构建 `data.js` 与依赖图 `graph.json`（2362 节点，边 type=dependency）。
- 实现 D3 v7 力导向「科技树」可视化、分类检索、时间轴、来龙去脉推演等核心视图。
- 打包 `tech-evolution-db-v1.0`，配套 `tools/` 工具集（gen_techs / disassemble / relink_parents / extend_net / complete_net / writeback 等）。

**涉及文件**：`assets/data.js`、`assets/style.css`、`assets/core.js`、`assets/app.js`、`tools/`

---

### 变更类型图例

| 类型 | 含义 |
|---|---|
| `milestone` 里程碑 | 关键阶段交付，项目形态发生质变 |
| `feature` 功能 | 新增站点 / 页面 / 能力 |
| `data` 数据 | 数据规模与结构扩充 |
| `fix` 修复 | 缺陷修正 |
| `refactor` 重构 | 结构 / 代码重构（不改变对外行为） |
| `docs` 文档 | 文档 / 说明补充 |
