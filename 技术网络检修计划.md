# 技术网络检修计划

> 目标：系统性修正「技术空缺 / 中间技术缺失 / 关联错误牵强」三类问题。
> 方式：分批推进，每批小规模、可独立验收、可回滚、独立登记提交。
> 基线日期：2026-09-13（v0.9.21 之后）

---

## 一、体检结论（为什么要检修 · 实测数据）

当前主管线规模：**2,288 个节点 / 6,139 条依赖边**（平均前置 1.82–3.21 条）。
以下全部为脚本实测，非估计。

### 1. 泛化学理枢纽「过度承重」

全库有 14 个真正意义上的跨界泛化枢纽：

| 枢纽 | 下游数 | 枢纽 | 下游数 |
|---|---|---|---|
| 数学 mathematics | 637 | 数值分析 numerical | 105 |
| 化学 chemistry | 433 | 最优化/线性规划 | 105 |
| 经典力学 physics | 289 | 分子生物学 | 94 |
| 算法理论 algorithm | 214 | 材料科学 materials | 93 |
| 统计学 statistics | 192 | 遗传学 genetics | 89 |
| 工程学 engineering | 170 | 免疫学 immunology | 88 |
| 文字 writing | 114 | 语言 language | 12 |

**【A 族】563 个节点（占全库 25%）的全部前置都落在这 14 个枢纽内**，没有任何领域内前置：

| 分类 | A 族数量 |
|---|---|
| basic 科学基础 | 176 |
| material 材料 | 96 |
| info 信息 | 92 |
| life 生命 | 80 |
| manufact 制造 | 68 |
| energy 能源 | 48 |
| build / military | 2 / 1 |

> 判定时要分两层：**A1 = 学理前置本身合理、但缺领域前置（补）**；**A2 = 学理前置本身就不合理（改/删）**。
> 例：`电池 ← 化学` 属 A1（补「伏打电堆/电解」等）；`造纸 ← 文字` 属 A2（应改指纤维处理/帘抄工艺）。

### 2. 关联牵强最集中处

**【E 族】173 个节点「只有 1 个前置、且该前置是泛化枢纽」**：

| 分类 | E 族数量 | 说明 |
|---|---|---|
| info 信息 | 91 | 大量 `← 数学` |
| **manufact 制造** | **68** | **占该类 163 个节点的 42%** |
| life / basic / material / energy / build | 5 / 2 / 2 / 2 / 2 | |
| military | 1 | |

典型病灶（manufact 类）：镗床、刨床、钻床、数控机床、加工中心、车铣复合、激光切割、水射流切割、等离子切割、电火花加工、线切割、压铸、模锻、铆接、电弧焊、气焊、电阻焊、激光焊、搅拌摩擦焊、电子束焊、钎焊 —— **前置一律是「数学」**。正确前置应是机床/切削工艺/冶金/电学，数学最多算辅助。

**【F 族】27 条「跨分类 + 年份跨度 > 800 年」的边**，其中明显错误：

- **`马蹄铁(transport,300)` → 10 个军事节点**：火炮、板甲、火绳枪、滑膛枪、手枪、地雷、手榴弹、火箭弹、加农炮、防弹衣 —— **硬错误，10 条全删**（这是生成器「跨类前提」槽位的残留）
- `光学(basic,1000)` → 8 条：全息显示、超材料、定向能武器、数码相机/图像传感器、发光二极管、激光器、激光、显示技术 —— 应经「光电效应 / 半导体光电器件」等中介，不宜直连
- `造纸(105)` → 钢笔(1884)：牵强
- `平板玻璃(100)` → 自然采光(1800)：牵强
- `印刷术(1040)` → 心理战(1900)：牵强
- `火药(850)` → 火箭/钻爆法/隧道掘进：**概念成立，保留**（属合理的跨域传导）

### 3. 中间技术缺失

- **`assets/techs_midtech.js` 共 96 条中间技术案例，完全没有并入网络**：字段只有 `id/name/en/date/summary/born/backgrounds`，**没有 `category`、没有 `dependsOn`**，只挂在独立的 `midtech.html` 上展示，不参与科技树。
- **历史断链 267 条**：下游依赖数为 0、且年份 < 1900 的节点（历史上明明有后续演化，链却断了）：

| 分类 | material | basic | energy | transport | manufact | build | military | life | info |
|---|---|---|---|---|---|---|---|---|---|
| 断链数 | 44 | 42 | 34 | 33 | 30 | 26 | 25 | 23 | 10 |

> 最刺眼的是 **basic 类 42 条**：三角学、对数、概率论、群论、集合论、图论、数理逻辑、纽结理论、天体力学、分析力学、电磁理论、波动光学、原子论、晶体学、元素周期律、化学键… **全部是叶子节点** —— 数学/科学各分支之间的前后依赖链整段缺失。
> 这才是「缺中间技术」最直接的量化证据：不是缺节点，而是**缺把它们串起来的中介环节**。

### 4. 结构元数据问题

- **重名节点 23 组**（同一技术重复建节点），例如：
  `集成电路→ic/chip`、`锻造→mat_forging/smithing`、`自修复材料→mat_selfheal/self_healing`、`木炭→ene_charcoal/charcoal`、`固态电池→ene_solidstate/solid_state_battery`、`神经网络→inf_nn/neural_network`、`虚拟现实→inf_vr/vr`、`增强现实→inf_ar/ar`、`脑机接口→bio_bci/bci`、`合成生物学→bio_synbio/synthetic_biology`、`定向能武器→mil_directedenergy/directed_energy` 等 23 组。
- **孤立节点 2 个**：`钻木取火(energy,-200000)`、`手势交流(basic,-100000)` —— 既无前置也无下游，等于不存在。
  （另需复核：`钻木取火` 与起点 `用火(-1000000)` 是否重复；`天然磁石(-1000)` 作为 8 大文明起点之一是否妥当。）
- **纪元取值不统一**：库里同时存在 `industrial(262)`/`electrical(212)`/`info(744)`/`intelligent(686)` 与 `classical(30)`/`medieval(29)`/`earlymodern(46)`/`future(132)` —— 两套纪元体系混用，`electrical` 与 `electric`、`info` 与 `information` 亦有混写。
- **近现代占比失衡**：`intelligent(686) + info(744) = 1,430`，占全库 **62%**；而 `manufact` 在 classical/medieval/earlymodern 三期合计仅 7 个节点。
- **两套管线严重不一致**：全量管线 `analysis-engine/data/graph.json` = **2,299 节点 / 7,539 条边**，主管线 = 2,288 / 6,139。
  - 1,689 条边只存在于 graph.json（`dependency` 1,598 + `enables` 91）；404 条只在主管线。
  - graph.json 还独占 11 个节点：`calendar_agri`、`pottery_wheel`、`ic_tooling`、`time_standard`、`icbm`、`comsat`、`earth_obs`、`film`、`copilot`、`food_preserve`、`precision_breeding`（其中 `calendar_agri` 是已知待清理的脏 id）。
  - 影响：`tree_full.html` 展示的仍是旧数据。

### 5. 涉及但未纳入本计划的已知遗留

- 1,027 条 `upgrade_stubs` 三段式模板文案待重写（v0.9.21 登记）；
- 生成器 `tools/gen_techs.js · gen_run.js · upgrade_stubs.js` 无保护，重跑会复现伪边；
- 12 条 `enables` 悬空引用；
- 部分节点「直接催生了 X、Y」列表与当前反图不一致（反向索引陈旧）。

---

## 二、判定标准（每条边/每个节点都按此四条过）

新增或保留一条「A 是 B 的前置」，必须同时满足：

1. **必要性** —— 去掉 A，B 的出现是否会受实质影响？（只是「同处一个时代/同一大类」→ **不成立，删**）
2. **排他性** —— A 是否为该环节最直接、最合理的先行者？有更贴近的前置时，优先换掉泛化枢纽。
3. **时序性** —— A 的年份必须早于 B（或同期工艺可达）。
4. **域内优先** —— 优先同领域前置；跨领域必须有**明确的传导机制**（如 `火药 → 枪炮` 成立，因为火药是发射药本身）。

**分层处置**：`保留` / `改前置` / `删边` / `降级为概念影响`（不进 dependsOn，只写进文案）。

---

## 三、分步检修计划

### 阶段 0 · 基线冻结与防再生（第 0 批）

**目的：让后面每一批都有可比的验收尺子，并防止修完又被生成器污染。**

| 批次 | 内容 | 产出 |
|---|---|---|
| 0.1 | 把本次体检固化为 `tools/audit_net.js`（输出 A/E/Ch/F 族计数、重名、孤立、覆盖矩阵、双管线差异），一条命令复跑 | 新脚本 |
| 0.2 | 建判定台账 `audit/ledger.json`：每条边一条记录 `{edge, verdict, reason, batch, date}`，脚本断言「候选 = 保留 + 改 + 删 + 降级」，不允许漏判 | 新文件 |
| 0.3 | 生成器加锁：`tools/gen_run.js` 开头加「同分类槽位边上限」断言 + `--no-write` 干跑开关，防止误覆盖线上数据 | 脚本改动 |
| 0.4 | 门禁扩充：pre-commit 追加结构指标断言（A/E/F 族计数不得上升），先设为告警 | `tools/check_docs.js` 或新钩子 |

验收：`node tools/audit_net.js` 输出的基线数字与本文件一致；网络数据零改动。
提交：1 次（仅工具）。

---

### 阶段 1 · 止血：硬错边与明显牵强关联清零（3 批）

**目的：先把「一眼假」的清掉，恢复可信度。**

| 批次 | 范围 | 预计 | 方法 |
|---|---|---|---|
| **1a** | F 族 27 条「跨类 + 跨度 > 800 年」 | 删 ~15 / 改 ~5 / 留 ~7 | 逐条按四标准判定；马蹄铁 10 条全删；光学 8 条改指光电效应/半导体器件；火药 3 条保留 |
| **1b** | 起点与孤立节点复核：8 个零前置起点 + 2 个孤立节点 | 改 ~3 | 确认文明起点是否名副其实；`钻木取火`/`手势交流` 补边或合并；`天然磁石` 起点地位复核 |
| **1c** | 23 组重名节点合并 | 删 23 个冗余节点 | 保留语义更准确的 id，把全库引用改指过去，再删冗余项；改完重跑环检测 |

验收：每批 `audit_net.js` 对比 + `check_net`（环 0 / 悬空 0 / 年倒挂 0）+ 文案一致性扫描。
提交：每批 1 次。

---

### 阶段 2 · 补肉：96 条中间技术并入网络（5 批）

**目的：把「缺中间技术」从根上补上 —— 不是再加叶子，而是**补中介**。

| 批次 | 范围 | 方法 |
|---|---|---|
| 2.1–2.5 | 96 条 midtech，每批约 20 条 | ① 给每条补 `category` / `era` / `year` / `dependsOn`（真实领域前置）；② 作为真实节点并入主管线（`techs_extra.js` 等）；③ **优先插在阶段 1 定位的 267 条历史断链处**（material 44 / basic 42 / energy 34 / transport 33 / manufact 30 / build 26 / military 25 / life 23 / info 10）；④ 更新 `midtech.html` 说明「已入网」 |

分批建议：`basic+material` → `energy+transport` → `manufact+build` → `life+military` → `info+剩余`。

验收：断链数下降、新增节点无孤立、环 0、文案与前置一致。
提交：每批 1 次。

---

### 阶段 3 · 补前置：E 族 173 + A 族 563（约 20 批）

**目的：把「学理枢纽过度承重」卸下来，换成真实的领域前置链。**

| 批次组 | 范围 | 重点 |
|---|---|---|
| **3a · E 族 173**（最高优先） | info 91 + manufact 68 + 其余 14 | manufact 68 个「←数学」的机床/焊接/铸造族，改指 机床/切削工艺/冶金/电学；info 91 条改指 电子学/计算机/通信 |
| **3b · A 族 basic 176** | 数学与科学各分支 | 建立**分支内部依赖链**：算术→代数→几何→微积分→…；天体力学←经典力学+天文学；化学键←原子论+晶体学 等 |
| **3c · A 族 material 96 + life 80** | | 材料工艺链（矿石→冶炼→合金→特种材料）；生命科学链（解剖→生理→病理→药理） |
| **3d · A 族 info 92 + manufact 68 + energy 48** | | 通信/计算分层；能源转换链（燃料→热机→发电→电网） |

每批 30–40 条，逐条按四标准判定，全部落台账。
验收：A/E 族计数逐批下降（目标：A 族 563 → <150，E 族 173 → 0）；环 0。
提交：每批 1 次。

---

### 阶段 4 · 补空缺与结构治理（4 批）

| 批次 | 内容 |
|---|---|
| 4a | **覆盖矩阵空缺判定**：`manufact` 在 classical/medieval/earlymodern 仅 7 个、`info` 在 prehistoric/classical/earlymodern 为 0 —— 逐格判定「真空缺（补节点）」还是「分类归属错误（改分类）」 |
| 4b | **纪元体系统一**：合并 `electrical/electric`、`info/information` 混写；确定 6 段制还是 9 段制；裁定 `future(132)` 的去留 |
| 4c | **近现代占比治理**：`intelligent+info = 62%`，评估是压缩近现代颗粒度，还是补齐古代/中世纪颗粒度 |
| 4d | **反向索引一致化**：全库反向索引（「直接催生了 X、Y」文案）与当前反图对齐；清 12 条 `enables` 悬空引用 |

---

### 阶段 5 · 全量对齐与收口（4 批）

| 批次 | 内容 |
|---|---|
| 5a | **两套管线对齐**：以主管线为准重建 `analysis-engine/data/graph.json`（现 2,299/7,539 → 对齐 2,288/6,139 口径），清 11 个 graph.json 独占节点（含脏 id `calendar_agri`），再重建 `assets/data_full.js` |
| 5b | **1027 条 `upgrade_stubs` 模板文案重写**（按句式定位，不全文替换） |
| 5c | **生成器语义硬约束**：把四标准里的「同分类不得直接连泛化枢纽」「跨类必须同域或有时序传导」写成生成期断言 |
| 5d | **全量回归 + 三处登记 + 上线**：`audit_net.js` 基线对比、`check_docs` 门禁、`run_model/run_deps/run_correction/run_changelog` 全绿；changelog + 纠错页 + 版本迭代日志三处登记 |

---

## 四、批次总览

| 阶段 | 批次 | 主要对象 | 目标指标 |
|---|---|---|---|
| 0 基线冻结 | 1 | 工具与台账 | 可复跑体检 + 台账机制 |
| 1 止血 | 3 | 27 边 + 23 组重名 + 10 节点 | F 族 → 0 硬错 |
| 2 补中间技术 | 5 | 96 条 midtech | 断链 267 → <120 |
| 3 补前置 | ~20 | E 173 + A 563 | A → <150，E → 0 |
| 4 结构治理 | 4 | 覆盖矩阵 / 纪元 / 占比 / 反索引 | 纪元取值唯一、反图一致 |
| 5 全量对齐 | 4 | 双管线 + 文案 + 生成器 + 上线 | graph.json 与主管线一致 |
| **合计** | **约 37 批** | | |

---

## 五、工作纪律（每批固定动作）

1. **判定**：按四标准逐条判，结论立即写入 `audit/ledger.json`，脚本断言无漏判；
2. **应用**：只改目标文件，改前备份到 `/tmp`；生成文件用行级编辑，禁止整数组重排（避免数万行噪声 diff）；
3. **校验**：`audit_net.js` 指标 + `check_net`（环/悬空/倒挂/重边）+ 文案一致性扫描；
4. **登记**：需要时同步 `changelog_data.js` / `correction_data.js` / `版本迭代日志.md`；
5. **提交**：一批一提交，提交信息写明「族 + 处置数 + 删/改/留」；
6. **推送**：仍由你在 GitHub Desktop 点 Push。

---

## 六、建议的第一批

**阶段 1a —— F 族 27 条跨类长跨度边逐条判定。** 理由：

- 范围最小（27 条），一次能看完；
- 错误最明显（马蹄铁 → 火炮那 10 条不会有人辩护）；
- 能立刻验证「四标准 + 台账 + 验收」这套流程跑得通，为后面 30 多批定型。

确认后我就开工。
