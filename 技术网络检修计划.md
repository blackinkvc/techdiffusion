# 技术网络检修计划

| 项目 | 内容 |
|---|---|
| 文件性质 | 结构检修计划与执行台账 |
| 编制日期 | 2026-09-13 |
| 最近修订 | 2026-09-15 |
| 基线版本 | v0.9.21（2,288 个节点 / 6,139 条依赖边） |
| 当前规模 | 主管线 2,267 个节点 / **6,169** 条依赖边；全量管线 2,299 节点 / 6,963 边（`graph.json`）、13,318 节点 / 16,811 边（`data_full.js`） |
| 当前状态 | 阶段 0、1、2 已完成；阶段 5 的派生文本同步（5b）**及其补漏（5.2a）**、中间技术库对齐（5d）、全量管线遗留边同步（5a 之一）已完成；派生文案与结构指标已纳入 pre-commit 门禁；**阶段 3 进行中（3.1a 已于 2026-09-14 执行，其余批次待续）**；全量管线文案（5.2b）与剩余边（5.1d）已于 2026-09-14 裁定：共有节点以主管线为权威，处置改为「以主管线为源单向重建」并并入阶段 5e；**执行待阶段 3、4 之后**；2026-09-14 另按用户指派补建材料类缺失节点「玄武岩纤维」（阶段序列外，节点 2,265 → 2,266 / 边 6,091 → 6,094，详见修订记录与 v0.9.27）；**2026-09-14 阶段 3.1a 已执行** —— E 族 manufact 首批 27 条前置逐条判定并写盘（保留 2 / 改前置 1 / 删边 24 / 补前置 27，新增 56 条边），依赖边 6,094 → **6,126**，A 族 562 → 535、E 族 173 → 146、Ch 族 260 → 252，详见修订记录与 v0.9.28；**2026-09-14 阶段 3.1b 亦已执行** —— E 族 manufact 余 41 条前置逐条判定并写盘（保留 0 / 改前置 3 / 删边 38 / 补前置 41，实际新增 82 条边、移除 41 条），依赖边 6,126 → **6,167**，A 族 535 → **494**、E 族 146 → **105**（`A.manufact` 与 `E.manufact` 均由 41 → 0，E 族 manufact 槽位清空）、Ch 族 252 → **246**，详见修订记录与 v0.9.30；**2026-09-14 阶段 3.1c 草案已出（未写盘）** —— E 族 info 首批 18 条（通信、网络与标识设备）逐条判定（保留 0 / 改前置 1 / 删边 17 / 补前置 17 条，37 条边），预演依赖边 6,167 → 6,187；**2026-09-15 阶段 3.1d 草案亦已出（未写盘）** —— E 族 info 次批 16 条（信息记录、存储与音视频设备）逐条判定（保留 0 / 改前置 2 / 删边 14 / 补前置 16 条，30 条边），预演依赖边 6,167 → 6,183；另有 3.1c / 3.1d 两批草案完成纠错页与版本日志登记（v0.9.31）；**2026-09-15 按用户指派补建 info 类缺失节点「面向对象方法」（`inf_oop`，阶段序列外）** —— 节点 2,266 → **2,267**、依赖边 6,167 → **6,169**（新增前置边 2 条：`inf_compiler` 1952 + `inf_pl_fortran` 1957），并把「**以不贴切的替代充当前置**」总结为一类错误（根因＝节点缺失而非判定不严，解法＝新建具体节点），新增判定规则 **R5：不得以学科级顶点充当替身前置**，详见修订记录与 v0.9.32；**2026-09-15 阶段 3.1e 草案亦已出并按裁定修订（未写盘）** —— E 族 info 三批 18 条（基础软件与数据管理）逐条判定（保留 0 / 改前置 1 / 删边 17 / 补前置 18 条，34 条边），预演依赖边 6,169 → 6,185；三批（3.1c / 3.1d / 3.1e）待用户逐条审核；**2026-09-15 架构改造（批次 D0，v0.9.33）** —— 新增「维度层」把「归属」与「因果」分离：新增只读覆盖层 `assets/pages/dimension_data.js`（10 学科门类 / 88 技术任务域 / 三级兜底 / 例外表）与校验工具 `tools/gen_dimension.js`，14 个泛化枢纽全部转为「维度顶点」，处置档位由四档改**五档**（+ `toField` ＝ 删伪边 + 落回维度）；全库 2,267 条完成机械打标（主维度 + 任务域 + 副维度），**依赖网络零改动**（仍 2,267 节点 / 6,169 边）；D1 例外表 / D2 维度顶点入边处置 / D3 A 族按 `toField` 重判三项为待审草案 |
| 执行方式 | 分批推进，每批独立判定、独立验收、独立提交 |

## 修订记录

| 日期 | 修订内容 |
|---|---|
| 2026-09-13 | 首版编制：体检基线 + 判定标准 + 6 阶段分批方案 |
| 2026-09-13 | 删除主观表述，改为可复核数据；增补批次日期栏；补记阶段 0、阶段 1 执行结果 |
| 2026-09-13 | 阶段 0、阶段 1 执行完毕：补记 1a/1b/1c 处置计数与验收结果；更新当前规模；登记 v0.9.22 |
| 2026-09-13 | 阶段 2 重新裁定：核查确认原「96 条中间技术入网」前提不成立（96/96 已在主管线），方案撤销；更正 §1.8 误读；Ch 族目标并入阶段 3；对齐工作移入阶段 5d；登记 v0.9.23 |
| 2026-09-13 | 批次 5.0 取证：用户抽检 5 条关系经逐层核查确认全部为位置式伪边，量化四层残留；登记 v0.9.23 后续提交 |
| 2026-09-14 | **批次 5.1d / 5.2b 裁定（v0.9.26）**：① 权威归属 —— 三条取证（`graph.json` 与 `data_full.js` 对共有节点仅 1 条文案不同＝同一份拷贝；对照主管线 v0.9.23 差 105、v0.9.24 差 971、v0.9.25 差 1,429，漂移随清理轮次单调递增且起点近零；`SCORE_META.generatedAt` 与 v0.9.23 时点吻合）→ 共有 2,265 节点**以主管线为权威**，全量侧＝主管线 v0.9.23 时点快照。② 处置 —— 不作逐条打补丁，改为**以主管线为源单向重建** `graph.json` 与 `data_full.js`，与 5a「按主管线重建」、5.1d 边处置**合并为同一批**，执行时点排在**阶段 3、4 之后随 5e**；重建时须新增「共有节点文案差异 = 0」断言纳入 pre-commit。③ 独占节点 —— 11,053 条判为独立语料（约 11,030 条来自 `new_techs.json`）、**不并入主管线**，但须加 `provenance` 标注并在 `tree_full.html` 说明来源；23 条同名 id 变体加 `aliasOf` 而不合并。④ 29 条年份倒挂 —— 逐条核实全部语义成立，按既有四档中的「**降级为概念影响**」落地（写入 `theory_data.js` 覆盖层），**不新增 `rel:"retro"` 无判据通道**；重建前须先写入覆盖层，否则信息丢失。⑤ 登记 —— 纠错页新增 CR-2026-0914-full-pipeline-authority，登记版本 v0.9.26。详见 `audit/ledger.json` 的 `batches["5.2b"].ruling` |
| 2026-09-14 | 批次 5.1d / 5.2b 复核更正：重测未承认边口径（`dependsOn ∪ enables` = 1,116；仅 `dependsOn` = 1,207；原记 1,127）并逐条核实 29 条年份倒挂（全部语义成立、不删，冲突在节点 year 取最早实例的建模口径）；更正 5.2b 陈旧文案数 971 → **1,429**（原口径对照 v0.9.24，已被 v0.9.25 改写作废）、独占节点模板文案 7 → **21**、补记 23 条同名 id 变体；取证定位全量侧文案为「主管线 v0.9.23 时点快照」并据此裁定共有节点以主管线为权威。详见 `audit/ledger.json` |
| 2026-09-13 | 批次 5.1 执行：①**派生文本同步**（主管线 977 节点 3,313 处文案，按生成器同构公式重算，`tools/regen_text.js`）；②**中间技术库对齐**（84 条 `backgrounds` + 69 条摘要，`tools/regen_midtech.js`）；③**全量管线遗留边同步**（576 条 eaa086f 已判定删除的伪边从 `graph.json` 与 `data_full.js` 两侧移除，`tools/prune_full_pipeline.js`）。新增批次 5.1d 记录全量管线剩余 1,127 条待逐条审核的边。登记 v0.9.24 |
| 2026-09-14 | **补建材料类缺失节点「玄武岩纤维」（v0.9.27，阶段序列外 · 用户指派）**：节点 2,265 → 2,266、依赖边 6,091 → 6,094。取 year=1985（连续纤维工业化，与同类节点 `mat_glassfiber` 取工业化起点的口径一致，且避免与其工艺母体构成年份倒挂）；前置 3 条（`chemistry` / `physics` / `mat_glassfiber`），经复核排除 `mining`（跨域误用）、`kiln`（排他性不成立）、`mfg_wiredraw`（金属拉丝）。文案由 `regen_text.js` 按族 A 公式重算（`mat_glassfiber` 因新增下游同步重算 3 处），重跑为不动点。**后续一侧经前提核查确认现有网络中无必要性成立的下游**（`mat_glassfiber` 零下游、`mat_frp` 仅 1 条下游；`bld_frpbridge` / `bld_carbonconc` / `bd_civ_130` 分别以玻璃钢、碳纤维为对象；year ≥ 1985 的 417 个 material/build/transport/energy 节点中无纤维增强下游），经用户裁定只补建该节点本身、后续侧登记为待补。`audit/baseline.json` 已按合法改进刷新。全量管线未改动（依 5.2b 裁定不逐条打补丁，随阶段 5e 重建带入）。登记 v0.9.27 |
| 2026-09-14 | **阶段 3.1a 执行（v0.9.28）**：E 族 manufact 首批 27 条（机床族 17 + 焊接 / 连接族 10）的前置由「`mathematics` 单前置」逐条判定并写盘 —— 保留 2（`mfg_cnc` / `mfg_cmm`，插补与坐标解算属定义性构成）/ 改前置 1（`mfg_gauge` 量具 → `numerals` 数字符号系统）/ 删边 24 / 降级 0；同时按同族可考事实补前置 27 条（共新增 56 条边、涉及 37 个前置 id），依赖边 6,094 → **6,126**（净 +32），节点 2,266 不变。派生链按序重算：`regen_text --apply` 45 节点 136 处、`regen_midtech --apply` 3 条，复跑均为不动点；结构问题族下降 A 562 → **535**、E 173 → **146**、Ch 260 → **252**（`Ch.manufact` 29 → 23）、`A.manufact` 68 → **41**、`mathematics` 前置 636 → **611**。三道门禁全绿，`audit/baseline.json` 刷新 12 项。批次 `3.1a` 由 `draft-ready` 转 `applied` 并补 `verification` 7 条；纠错页新增 CR-2026-0914-manufact-math-hub。**根治手段在阶段 5c**（生成器语义硬约束），本批属事后清理。顺延：E 族 manufact 其余 41 条（规则 R1–R4 沿用）、`mfg_resistweld` 年份待阶段 4、stub 字段质量批、同义重复节点对批、补节点候选 3 项。登记 v0.9.28 |
| 2026-09-14 | **阶段 3.1b 执行（v0.9.30）**：E 族 manufact 余 41 条的前置由「`mathematics` 单前置」逐条判定并写盘 —— 保留 **0** / 改前置 3（`mfg_sixsigma` → `statistics` + `mfg_spcc`、`mfg_adaptive` → `control_theory` + `mfg_servo`、`mfg_aps` → `optimization` + `computer`）/ 删边 38 / 降级 0；补前置 41 条目共 79 条边，连同 repoint 的 3 条 `newParent` 实际新增 82 条边、移除 41 条 `mathematics`，依赖边 6,126 → **6,167**（净 +41），节点 2,266 不变。**保留数为 0 是本批的判据性结论**：41 条逐条按四标准审核后无一条以学科级「数学」为定义性构成，说明该槽位不区分必要与非必要、属批量赋值为学科级顶点的产物；四标准中的**排他性**是筛出它们的唯一判据（时序校验对它们整体通过）。派生链按序重算：`regen_text --apply` 191 条（`summary` 65 / `significance` 58 / `views` 68）、`regen_midtech --apply` 后 95 条目，复跑均为不动点；结构问题族下降 A 535 → **494**、E 146 → **105**（`A.manufact` 与 `E.manufact` 均由 41 → 0）、Ch 252 → **246**、`mathematics` 前置 611 → **570**。三道门禁全绿，`audit/baseline.json` 刷新 10 项（`edges` 6,126 → 6,167、`dual.onlyInGraphJson` 1,229 → 1,270、`dual.onlyInMain` 508 → 590 等）。**核账陷阱（本批暴露）**：新增边须按「Σ `backfills.add` + Σ(repoint 各 1 条 `newParent`)」计 —— 仅按 `backfills` 合计 79 条推算会得 6,164，与实测 6,167 差 3。批次 `3.1b` 由 `draft-ready` 转 `applied` 并补 `verification` 11 条；纠错页新增 CR-2026-0914-manufact-math-hub-3.1b。**根治手段在阶段 5c**（生成器语义硬约束），本批属事后清理。顺延：补节点候选新增 2 项（高压流体加压设备、机器视觉）、同义重复节点对新增 1 例（`mfg_assembly` / `assembly_line`）。登记 v0.9.30 |
| 2026-09-15 | **架构改造「维度层」（v0.9.33，批次 D0）**：裁定走「**删边 + 归属标签**」（与 `theory_data.js` 已上线的「保边 + 标 `theory.latent` 虚边」不能同时成立），并确认六项设计 —— 两条轴并存（`sub` 技术任务域 88 域 / `field` 学科归属 10 门类）、主维度单值 + 副维度多值、14 个泛化枢纽全部转「维度顶点」、同意「`id` 前缀 + `category` 机械兜底 + 例外逐条覆盖」、「学理」并入学科维度、处置档位四档改五档（+ `toField`）。新增 `assets/pages/dimension_data.js` + `tools/gen_dimension.js`，全库 2,267 条机械打标完成（主维度路径 prefix 1,838 / cat 255 / prefix:bx 160 / hub-head 14；门类分布 eng 857 / life 339 / info 283 / matter 250 / mil 237 / math 126 / phys 90 / symbol 69 / chem 10 / earth 6）；`tools/apply_ledger.js` 加第五档并扩充完整划分断言；`core.js` 加 `buildDimensionBlock`；16 页挂载 + 版本串 `20260915a`。**依赖网络零改动**（2,267 / 6,169），基线不刷新。两处约束收紧：关键词匹配限定同 `category` 域内且长度 ≥ 3、一段前缀须与 `category` 同类（修正前 24 项不同类，修正后 0 例外）。三道门禁全绿、渲染校验 38/38。D1 / D2 / D3 三项待审草案。详见 `audit/ledger.json` 的 `batches["D0"]` |

---

## 一、体检基线（实测数据）

测量对象：主管线（`assets/data.js` + `techs_extra.js` + `techs_extend.js` + `techs_more.js`）。
规模：**2,288 个节点 / 6,139 条依赖边**，各类平均前置数 1.82–3.21。
以下数值由 `tools/audit_net.js` 计算，可复跑。

> 注：本章数字为**阶段 1 执行前**的体检基线（即问题规模）。`audit/baseline.json` 为**阶段 1 执行后**的快照，用作后续批次的回归门禁（指标不得劣化），二者口径不同、数值亦不同，不应互相校验。

### 1.1 泛化学理枢纽的下游集中度

下游依赖数 ≥ 80 的跨界枢纽共 14 个：

| 枢纽 | 下游数 | 枢纽 | 下游数 |
|---|---|---|---|
| 数学 mathematics | 637 | 数值分析 numerical | 105 |
| 化学 chemistry | 433 | 最优化/线性规划 | 105 |
| 经典力学 physics | 289 | 分子生物学 molecular_bio | 94 |
| 算法理论 algorithm | 214 | 材料科学 materials | 93 |
| 统计学 statistics | 192 | 遗传学 genetics | 89 |
| 工程学 engineering | 170 | 免疫学 immunology | 88 |
| 文字 writing | 114 | 语言 language | 12 |

**A 族定义**：节点的全部真实前置均落在上述 14 个枢纽内。

| 分类 | basic | material | info | life | manufact | energy | build | military | 合计 |
|---|---|---|---|---|---|---|---|---|---|
| A 族数量 | 176 | 96 | 92 | 80 | 68 | 48 | 2 | 1 | **563** |

A 族占全库 25%。处置分两层：**A1** = 学理前置成立但缺领域前置（补）；**A2** = 学理前置不成立（改或删）。
例：`电池 ← 化学` 属 A1；`造纸 ← 文字` 属 A2。

### 1.2 单前置且前置为泛化枢纽

**E 族定义**：节点仅有一条真实前置，且该前置属上述 14 个枢纽。

| 分类 | info | manufact | life | basic | material | energy | build | military | 合计 |
|---|---|---|---|---|---|---|---|---|---|
| E 族数量 | 91 | 68 | 5 | 2 | 2 | 2 | 2 | 1 | **173** |

manufact 类共 163 个节点，其中 68 个（42%）属 E 族。涉及节点包括镗床、刨床、钻床、数控机床、加工中心、车铣复合、激光切割、水射流切割、等离子切割、电火花加工、线切割、压铸、模锻、铆接、电弧焊、气焊、电阻焊、激光焊、搅拌摩擦焊、电子束焊、钎焊，其前置均登记为「数学」。

### 1.3 跨分类长跨度边

**F 族定义**：跨分类、年份跨度 > 800 年、且两端均非泛化枢纽。共 27 条，全部列出：

| 跨度 | 依赖者 | 前置 | 初步判定 |
|---|---|---|---|
| 1779 年 | 钢笔 info 1884 | 造纸 material 105 | 待判 |
| 1700 年 | 自然采光 build 1800 | 平板玻璃 material 100 | 待判 |
| 1200 年 | 火绳枪 military 1500 | 马蹄铁 transport 300 | 错误 |
| 1200 年 | 滑膛枪 military 1500 | 马蹄铁 transport 300 | 错误 |
| 1200 年 | 手枪 military 1500 | 马蹄铁 transport 300 | 错误 |
| 1200 年 | 防弹衣 military 1500 | 马蹄铁 transport 300 | 错误 |
| 1100 年 | 手榴弹 military 1400 | 马蹄铁 transport 300 | 错误 |
| 1076 年 | 火箭 transport 1926 | 火药 military 850 | 成立 |
| 1035 年 | 全息显示 info 2035 | 光学 basic 1000 | 待判 |
| 1030 年 | 超材料 material 2030 | 光学 basic 1000 | 待判 |
| 1030 年 | 定向能武器 military 2030 | 光学 basic 1000 | 待判 |
| 1000 年 | 火炮 military 1300 | 马蹄铁 transport 300 | 错误 |
| 1000 年 | 板甲 military 1300 | 马蹄铁 transport 300 | 错误 |
| 993 年 | 钻爆法 build 1843 | 火药 military 850 | 成立 |
| 980 年 | 定向能武器 military 1980 | 光学 basic 1000 | 待判 |
| 975 年 | 数码相机 / 图像传感器 info 1975 | 光学 basic 1000 | 待判 |
| 962 年 | 发光二极管 info 1962 | 光学 basic 1000 | 待判 |
| 960 年 | 激光器 info 1960 | 光学 basic 1000 | 待判 |
| 960 年 | 激光 info 1960 | 光学 basic 1000 | 待判 |
| 960 年 | 显示技术 info 1960 | 光学 basic 1000 | 待判 |
| 950 年 | 隧道掘进 build 1800 | 火药 military 850 | 成立 |
| 935 年 | 印刷术 info 1040 | 造纸 material 105 | 成立 |
| 900 年 | 地雷 military 1200 | 马蹄铁 transport 300 | 错误 |
| 900 年 | 火箭弹 military 1200 | 马蹄铁 transport 300 | 错误 |
| 900 年 | 加农炮 military 1200 | 马蹄铁 transport 300 | 错误 |
| 900 年 | 水磨 manufact 1000 | 水车 energy 100 | 成立 |
| 860 年 | 心理战 military 1900 | 印刷术 info 1040 | 待判 |

合计：判定为错误 10 条（全部为 `马蹄铁 → military`），成立 5 条，待判 12 条。

### 1.4 历史断链

**Ch 族定义**：下游依赖数为 0，且年份 < 1900。

| 分类 | material | basic | energy | transport | manufact | build | military | life | info | 合计 |
|---|---|---|---|---|---|---|---|---|---|---|
| Ch 族数量 | 44 | 42 | 34 | 33 | 30 | 26 | 25 | 23 | 10 | **267** |

basic 类 42 条包含三角学、对数、概率论、群论、集合论、图论、数理逻辑、纽结理论、天体力学、分析力学、电磁理论、波动光学、原子论、晶体学、元素周期律、化学键等，均为无下游的叶节点，即科学分支之间的依赖链缺失。

### 1.5 重复节点

同名节点共 23 组，每组为同一技术在两处登记：

`集成电路→ic/chip`、`锻造→mat_forging/smithing`、`自修复材料→mat_selfheal/self_healing`、`木炭→ene_charcoal/charcoal`、`固态电池→ene_solidstate/solid_state_battery`、`空间太阳能→ene_satpower/space_solar`、`无线输电→ene_wpt/wireless_power`、`数字孪生→mfg_digitaltwin/digital_twin`、`独木舟→tr_canoe/canoe`、`神经网络→inf_nn/neural_network`、`增强现实→inf_ar/ar`、`虚拟现实→inf_vr/vr`、`边缘智能→inf_edgeai/edge_ai`、`药理学→bio_pharmacology/pharmacology`、`组织工程→bio_tissueeng/tissue_engineering`、`mRNA 疫苗→bio_mrnavax/mrna_vaccine`、`基因治疗→bio_genetherapy/gene_therapy`、`脑机接口→bio_bci/bci`、`合成生物学→bio_synbio/synthetic_biology`、`器官芯片→bio_organonchip/organs_on_chip`、`精准医疗→bio_precisionmed/personalized_medicine`、`穹顶→bld_dome/dome`、`定向能武器→mil_directedenergy/directed_energy`

### 1.6 孤立节点与起点

- 零前置节点 8 个：语言、石器、用火、轮子、石器打制、钻木取火、手势交流、天然磁石。
- 既无前置也无下游（孤立）2 个：钻木取火、手势交流。
- 待复核：钻木取火与起点「用火」是否重复；天然磁石列为文明起点的依据。

### 1.7 纪元与分布

纪元取值两套体系混用：`prehistoric 53 · ancient 94 · classical 30 · medieval 29 · earlymodern 46` 与 `industrial 262 · electrical 212 · info 744 · intelligent 686 · future 132` 并存。

近现代占比：`intelligent 686 + info 744 = 1,430`，占 62%。
`manufact` 在 classical / medieval / earlymodern 三期合计 7 个节点。

### 1.8 中间技术库

`assets/techs_midtech.js` 共 96 条（2026-09-13 修复库内重复条目后为 95 条），字段为 `id / name / en / date / summary / born / backgrounds`。

**更正（2026-09-13 核查）**：本节初版写「无 `category`、无 `dependsOn`，未进入主管线」，属**误读**。实情是——这 96 条的 `id` 与名称**全部已在主管线中**（连 `data.js` 的 4 条手工案例也都在），它们不是待入网的新技术，而是 `tools/gen_midtech.js` 按「跨领域汇聚」口径**从主管线筛出的案例视图**；缺 `category / dependsOn` 只是因为该视图层只携带展示所需字段。原始误读已导致阶段 2 的方案设计无效，详见批次 2.1 的重新裁定。

`backgrounds` 是生成时刻（2026-08-26）主管线 `dependsOn` 的**快照**（`gen_midtech.js:190-195` 原样搬运），此后主管线经 v0.9.20 / v0.9.21 清理，两者已偏离：96 条中完全一致 11 条、含已删前置 44 条、缺新前置 1 条、双向偏离 40 条。

### 1.9 双管线差异

| 数据集 | 节点 | 边 |
|---|---|---|
| 主管线 | 2,288 | 6,139 |
| `analysis-engine/data/graph.json` | 2,299 | 7,539 |

- 仅存在于 graph.json 的边 1,689 条（`dependency` 1,598 + `enables` 91）；仅存在于主管线的边 404 条。
- graph.json 独占节点 11 个：`calendar_agri`、`pottery_wheel`、`ic_tooling`、`time_standard`、`icbm`、`comsat`、`earth_obs`、`film`、`copilot`、`food_preserve`、`precision_breeding`（含待清理的合成 id `calendar_agri`）。
- 影响：`tree_full.html` 展示的数据与主管线不同源。

> **本表为阶段 1 前的基线值。** 批次 5.1 后实测（`tools/audit_full_pipeline.js`）：`graph.json` 2,299 节点 / **6,963** 边，`data_full.js` 13,318 节点 / **16,811** 边；两侧节点数均未变动。`tools/audit_net.js` 的 `dual` 指标（与上表口径不同，不可互校）：`onlyInGraphJson` 1,779 → **1,204**，`onlyInMain` **448** 未变。

### 1.10 其他已登记遗留

- ~~1,027 条 `upgrade_stubs` 三段式模板文案待重写~~ → **批次 5.1 已完成**，实际重写 **3,313 处**（977 个节点的 `summary` / `significance` / `views[].text`），方法见 §三 阶段 5b；
- 生成器 `tools/gen_techs.js · gen_run.js · upgrade_stubs.js` 无输入保护，重跑会重现已删除的边；其中 `gen_run.js` 已于阶段 0.3 加装护栏（`--no-write` 干跑、同分类槽位边上限断言默认为 0、写前自动备份），`gen_techs.js` 与 `upgrade_stubs.js` 的语义硬约束见批次 5c；
- 12 处 `enables` 悬空引用（指向 11 个概念占位节点，`icbm` 被 `rocket` 与 `missile` 各引用一次）；
- ~~部分节点的「直接催生了 X、Y」文案与当前反图不一致~~ → **批次 5.1 已完成**（下游枚举句与反图重算一致），**批次 5.2 补漏**（识别器盲区导致 38 节点漏改 + 另两条生成链共 423 处未覆盖）；
- **`views[0].period` 与 `ERAS` 表纪元名不符 99 处**（如 `language` 的 period 为「史前」而 `era=prehistoric` 对应「史前时代」；`mathematics` 为「古代」而应为「古代文明」）→ 判为命名口径不一致而非结构错误，并入「纪元体系统一」批次，与既有的 52 项 era-vs-year 不匹配同批处理；
- **全量管线 `data_full.js` 的 1,429 条陈旧文案**（对共有节点持有的第三份独立副本，原记 971 条系对照 v0.9.24 的口径、已被 v0.9.25 的 531 处改写作废）→ 批次 5.2b；2026-09-14 复核已裁定共有节点以主管线为权威，待用户确认处置方向。

---

## 二、判定标准

新增或保留一条「A 是 B 的前置」，须同时满足：

1. **必要性**：去掉 A，B 是否仍能按史实出现。仅因「同处一个时代或同一大类」而建立的关系不成立。
2. **排他性**：A 是否为该环节最直接的先行者。存在更贴近的前置时，替换泛化枢纽。
3. **时序性**：A 的年份早于 B，或同期工艺可达。
4. **域内优先**：优先同领域前置。跨领域须给出明确传导机制（`火药 → 枪炮` 成立，火药即发射药）。

处置**五档**：`保留` / `改前置` / `删边` / `降级为概念影响`（不写入 dependsOn，仅见于文案）/ **`落回维度 toField`**（第五档，2026-09-15 新增）。

**第五档 `toField` 的判据与用法**（规则 R8）：当一条边唯一成立的解释是「学科归属」而非「必要条件」时，处置为**删边 + 把归属记入维度层**，而不是硬找替身前置。与第三档 `删边` 的区别在于它必须给出归属落点（执行器强制校验 `e.field`）。

**三条配套规则**：**R6** 归属不写入 `dependsOn` —— 「A 属于数学领域」不是「A 依赖数学」；**R7** 学科级顶点转维度顶点 —— 14 个泛化枢纽不再作为前置，只作归属锚点；**R8** 不得以学科级顶点充当替身前置（**R5** 的加强版：R5 禁「换名字」，R8 给出正面出口）。

**第五档的边界（须守住）**：`toField` **不是**「删边的通用免责条款」。它只适用于「归属型」边；对确有域内必要条件的节点，仍须走 `改前置` 或 `删边 + 补前置`（如 3.1a–3.1e 已裁定的 93 条）。判定时须回答：去掉该学科级顶点后，本节点是否仍按史实出现？若能，且本节点确有域内前置 → `delete + backfill`；若能，且本节点的**唯一**「前置」就是归属 → `toField`。

---

## 三、分批计划与执行记录

### 阶段 0 · 基线冻结与防再生

| 批次 | 内容 | 日期 | 状态 |
|---|---|---|---|
| 0.1 | 体检固化为 `tools/audit_net.js`，输出 `audit/baseline.json` | 2026-09-13 | 已完成 |
| 0.2 | 建判定台账 `audit/ledger.json`，脚本断言「候选 = 保留 + 改 + 删 + 降级」 | 2026-09-13 | 已完成 |
| 0.3 | 生成器加保护：`gen_run.js` 加同分类槽位上限断言与 `--no-write` 干跑开关 | 2026-09-13 | 已完成 |
| 0.4 | 门禁扩充：`audit_net.js --check` 结构指标回归 | 2026-09-13 | 已完成 |

### 阶段 1 · 止血：错误边与重复节点

| 批次 | 内容 | 日期 | 状态 |
|---|---|---|---|
| 1a | F 族 27 条跨分类长跨度边逐条判定 | 2026-09-13 | 已完成 |
| 1b | 8 个零前置起点 + 2 个孤立节点复核 | 2026-09-13 | 已完成 |
| 1c | 23 组重复节点合并 | 2026-09-13 | 已完成 |

**执行结果（2026-09-13）**

| 批次 | 处置 | 计数 |
|---|---|---|
| 1a | 删边 | 15（`inf_pen←paper`；`mil_plate`/`mil_arquebus`/`mil_musket`/`mil_pistol`/`mil_bodyarmor`/`mil_landmine`/`mil_rocketartillery`/`mil_cannon`/`mil_grenade`←`tr_horseshoe`；`metamaterial`/`directed_energy`/`mil_directedenergy`/`display`←`optical`） |
| 1a | 改前置 | 4（`holography`←`wave_optics`；`led`/`lasers`/`laser`←`quantum_mech`） |
| 1a | 保留 | 8（跨域但有明确传导机制者，含 火药→火箭 / 钻爆法） |
| 1a | 补真实前置 | 10（板甲←钢铁+锻造；火绳枪/滑膛枪/手枪←火药+铁器+螺纹；防弹衣←板甲+钢铁；地雷←火药+铁器；火箭弹←火药+箭；加农炮←火药+青铜铸造；手榴弹←火药；自然采光←浮法玻璃+建筑学） |
| 1b | 补起点前置 | 4（轮子←木作；石器打制←石器；钻木取火←石器+用火；语言←手势交流） |
| 1c | 合并重复节点 | 23 组（并入冗余项独有前置 7 条、按时序性剔除 4 条、引用改指 19 处，残留引用 0） |

验收：节点 2,288 → 2,265；依赖边 6,139 → 6,091；F 族 27 → 8；同名重复组 23 → 0；零前置 8 → 4；孤立 2 → 0；A 族 563 → 562；Ch 族 267 → 260；环 / 自环 / 重复边 / 悬空前置 / 年份倒挂均为 0。每批写盘经五道门（台账完整划分断言 → 时序性校验 → 环校验 → 逐节点写后断言 → 残留引用检查）逐批通过。

### 阶段 2 · 中间技术库性质核查与防再生（原「中间技术入网」已撤销）

| 批次 | 内容 | 日期 | 状态 |
|---|---|---|---|
| 2.1 | 原方案前提核查与重新裁定（96 条非新增技术；撤销「入网」） | 2026-09-13 | 已完成 |
| 2.2 | 修复库内重复条目 `mil_smartmunition`（96 → 95 条） | 2026-09-13 | 已完成 |
| 2.3 | `gen_midtech.js` 加护栏（重复 id / 断链背景即中止；`--no-write`；写前备份；漂移报告） | 2026-09-13 | 已完成 |
| 2.4 | 中间技术库与主管线对齐 | —— | 移交阶段 5 |

**撤销理由（批次 2.1 台账 `audit/ledger.json` 存证）**：

1. **重叠 96/96**——中间技术库的每一条 `id` 都已在主管线中，`gen_midtech.js` 的头部注释与数据源（`TECHS`）均表明它是案例视图而非新技术集合。按原方案执行会新增 96 个重复节点。
2. **库内重复**——`mil_smartmunition` 登记两次（index 50 / 57），两条内容完全不同。
3. **数据过期**——`backgrounds` 是 2026-08-26 版 `dependsOn` 的快照，85/96 已与当前主管线偏离。
4. **改写时序**——95 个唯一 id 中 69 条的摘要含前置枚举句且与当前前置不符，其中多数属 A / E 族（前置待阶段 3 补齐）；现在改写会在阶段 3 后重写第二遍。
5. **生成器不可复现**——以当前数据重跑 `gen_midtech.js` 只产出 32 条（候选门槛 `dependsOn.length >= 4` 在清理后大部分节点已不满足），即原库无法按原口径重建。

**处置**：原「96 条入网」撤销；「重建 `backgrounds` + 重写 95 条摘要枚举句」移入**阶段 5**（结构定稿后一次性执行）；生成器口径修订同为阶段 5 前置事项。原寄望于本例压低 Ch 族的目标**改由阶段 3 承接**——Ch 族的成因是「无下游」，只能靠补 / 改前置解决（阶段 1a / 1b 已验证该机制有效），无法靠新增节点解决。

### 阶段 3 · 前置补齐（原「中间技术入网」的 Ch 族目标并入本阶段）

| 批次组 | 范围 | 计划日期 | 状态 |
|---|---|---|---|
| 3.1a | E 族 manufact 首批 27 条：机床族 17 + 焊接 / 连接族 10 | 2026-09-13 | **已完成（v0.9.28）**：保留 2 / 改前置 1 / 删边 24 / 降级 0，补前置 27 条（+56 条边）；依赖边 6,094 → 6,126 |
| 3.1b | E 族 manufact 余 41 条：切割 / 成形 / 热处理与表面强化 / 特种加工 / 装配与工装 / 自动化部件与机器人 / 生产组织与数字化 | 2026-09-14 | **已完成（v0.9.30）**：保留 0 / 改前置 3 / 删边 38 / 降级 0，补前置 41 条（实际新增 82 条边、移除 41 条）；依赖边 6,126 → **6,167** |
| 3.1c | E 族 info 首批 18 条：通信、网络与标识设备（信息传输介质 / 网络设备与网络服务 / 标识设备） | 2026-09-14 | **草案（待审核）**：保留 0 / 改前置 1 / 删边 17 / 降级 0，补前置 17 条（37 条边）；预演依赖边 6,167 → 6,187 |
| 3.1d | E 族 info 次批 16 条：信息记录、存储与音视频设备（书写与记录介质 / 磁记录与存储 / 音视频设备 / 定位与可视化） | 2026-09-15 | **草案（待审核）**：保留 0 / 改前置 2 / 删边 14 / 降级 0，补前置 16 条（30 条边）；预演依赖边 6,167 → 6,183 |
| 3.1e | E 族 info 三批 18 条：基础软件与数据管理（操作系统与处理器 / 编程语言与编译系统 / 中间件与建模 / 数据库族 / 数据表示） | 2026-09-15 | **草案（待审核，已按 2026-09-15 用户裁定修订三处）**：保留 0 / 改前置 1 / 删边 17 / 降级 0，补前置 18 条（34 条边）；预演依赖边 6,169 → 6,185、E 族 105 → 87 |
| 3a | E 族 105 条（info 91、manufact 0、其余 14）；manufact 68 已全部处置（3.1a 27 条 + 3.1b 41 条）；info 已有 52 条出草案（3.1c 18 条 + 3.1d 16 条 + 3.1e 18 条） | 待定 | 进行中（3.1a、3.1b 已完成；3.1c / 3.1d / 3.1e 草案待审核；规则 R1–R4 沿用） |
| 3b | A 族 basic 176 条 | 待定 | 待执行 |
| 3c | A 族 material 96 + life 80 | 待定 | 待执行 |
| 3d | A 族 info 92 + manufact 68 + energy 48 | 待定 | 待执行 |
| 3e | Ch 族历史断链（原 267 条，阶段 1 后 260 条）补下游依赖 | 待定 | 待执行 |

目标：E 族 173 → 0；A 族 563 → 150 以下；Ch 族 260 → 120 以下。每批 30–40 条。

**首批病灶实测**：E 族 manufact 68 条的当前前置**全部为同一条 `mathematics`**，无一条例外——同一槽位被批量赋值。3.1a 取其机床族（镗床 / 钻床 / 刨床 / 砂轮 / 刀柄 / 切削液 / 电火花加工 / 数控机床 / 加工中心 / 线切割 / 电火花线切割机 / 车铣复合 / 三坐标测量 / 在机测量 / 刀具状态监控 / 低温切削 / 量具）与焊接 / 连接族（铆接 / 钎焊 / 胶接 / 电阻焊 / 电弧焊 / 气焊 / 电子束焊 / 激光焊 / 搅拌摩擦焊 / 焊接机器人），共 27 条；判定草案写入 `audit/ledger.json` 的 `batches["3.1a"]`，逐条给出 `newParent` + `reason`。**已写盘（2026-09-14，v0.9.28）**：保留 2（`mfg_cnc` / `mfg_cmm`，插补与坐标解算属定义性构成）/ 改前置 1（`mfg_gauge` 量具 → `numerals` 数字符号系统）/ 删边 24 / 降级 0；同时按同族可考事实补前置 27 条（共新增 56 条边、涉及 37 个前置 id），避免「只删不补」造成零前置。依赖边 6,094 → **6,126**、节点 2,266 不变。写盘由 `tools/apply_ledger.js --batch 3.1a --apply` 执行（定位 27 / 27、逐节点断言「现值 === 预期旧值」通过、写前备份至 `/tmp/bak_3.1a_1789328792963`）；派生链按序重算（`regen_text` 45 节点 136 处 → `regen_midtech` 3 条）并验不动点；批次状态由 `draft-ready` 转 `applied` 并补 `verification` 7 条。**其余 41 条形态相同，规则 R1–R4 可直接沿用。已写盘（2026-09-14，v0.9.30）**：保留 **0** / 改前置 3（`mfg_sixsigma` → `statistics` + `mfg_spcc`、`mfg_adaptive` → `control_theory` + `mfg_servo`、`mfg_aps` → `optimization` + `computer`）/ 删边 38 / 降级 0，补前置 41 条目共 79 条边；移除 41 条 `mathematics`、实际新增 82 条，依赖边 6,126 → **6,167**。**保留数为 0 是本批的判据性结论** —— 该槽位不区分必要与非必要，属批量赋值为学科级顶点的产物（与 `bx_*` 占位、stub 捏造链同类）；四标准中的排他性是筛出它们的唯一判据。

说明：Ch 族（下游依赖 = 0 且 year < 1900）的修复机制与 A / E 族同源——都是「改前置 / 补前置」使节点重新接入网络，故并入本阶段统一推进；原寄望于新增中间技术节点压低 Ch 族的方案已随阶段 2 撤销。

### 阶段 4 · 结构与元数据治理

| 批次 | 内容 | 计划日期 | 状态 |
|---|---|---|---|
| 4a | 覆盖矩阵空缺逐格判定（manufact 古典/中世纪、info 早期） | 待定 | 待执行 |
| 4b | 纪元体系统一并校对 | 待定 | 待执行 |
| 4c | 近现代占比评估 | 待定 | 待执行 |
| 4d | 反向索引与反图对齐；清 12 条 `enables` 悬空引用 | 待定 | 待执行 |

### 阶段 5 · 全量对齐与收口

| 批次 | 内容 | 计划日期 | 状态 |
|---|---|---|---|
| 5a | 全量管线对齐：<br>· **已完成部分**——把 v0.9.20/21（`eaa086f`）已判定删除、当时未同步全量管线的 **576 条**伪边从 `graph.json` 与 `data_full.js` 两侧移除（`tools/prune_full_pipeline.js`，清单存 `audit/full_pipeline_exclusions.json`）<br>· **待执行部分**——按主管线重建（**已由 5.2b 裁定吸收，见下**）、清 11 个独占节点 | **待定（排在阶段 3、4 之后）** | 部分完成；**待执行部分的时点与判定已由批次 5.2b 裁定确定**（2026-09-14） |
| 5b | 派生文本同步（原「1,027 条 `upgrade_stubs` 模板文案重写」）：按生成器同构公式从当前网络重算，**已完成 3,313 处**（977 节点的 `summary` / `significance` / `views[].text`），工具 `tools/regen_text.js`；含用户抽检的 3 条实例（`mfg_3dp`、`mil_loyalwingman`、`mat_graphene`）。**手写散文不匹配任何模板骨架，一律不动** | 2026-09-13 | **已完成**（覆盖不全，由 5.2a 补漏） |
| 5.2a | **派生文案补漏（三族）**：修模板识别器盲区（槽位通配排除了空白字符 → `ABS 树脂` / `工业 4.0` / `遗传学 / DNA` 等含空格名称所在整句被判为手写散文静默跳过）→ 族 A 补改 **108 处**；并把另两条此前完全未覆盖的生成链纳入同步 —— 族 B `assets/techs_extend.js`（`extend_net.js:400-425` `deepen()` 追加深化句，**34 处**，只重写尾段、手写 base 逐字保留）、族 C `assets/techs_more.js`（`gen_1000.js:344-358` `summarize()` 整句合成，**389 处**）。三族合计 **531 处 / 461 节点**。规则抽为 `tools/derived_text_rules.js` 单一真相源；`tools/regen_text.js` 升级为三族三文件并新增 `--check`。连带中间技术库摘要拷贝同步 **15 条**（派生链下游，顺序：主管线文案 → 中间库文案） | 2026-09-13 | **已完成** |
| 5c | 生成器语义硬约束（写入四标准中可自动化的部分） | 待定 | 待执行 |
| 5d | **中间技术库对齐**：按共享规则重建 95 条 `backgrounds`（**84 条变化**）+ 同步 `summary`（**69 条变化**），工具 `tools/regen_midtech.js`；规则抽为 `tools/midtech_rules.js` 供 `gen_midtech.js` 共用（原口径已不适用：当前重跑仅能选出 32 条且分类严重倾斜，故**案例集合保持人工策展、不重选**） | 2026-09-13 | **已完成** |
| 5e | 全量回归 + 三处登记 + 上线 | 待定 | 待执行 |
| 5.1d | **全量管线剩余残留审核**：`graph.json` / `data_full.js` 中仍有约 1,116 条边未被主管线承认（`dependsOn ∪ enables` 口径；仅按 `dependsOn` 判为 1,207，原记 1,127 属两口径之间、须统一后复算）＝ `yearInv` 29 + 位置式签名 705 + 其他 393，须按「生成器复现 + 逐边 diff + 语义判定」逐类处置。**2026-09-14 复核：29 条 `yearInv` 已逐条核实，全部可作「该技术后期阶段吸收了源技术」解读（采矿用炸药、犁用铁器、拱用水泥、计算机用集成电路、自动化用机器人与 AI…），非方向性错误、不删；冲突在「节点 year 取最早实例」的建模口径** | 待定 | **已裁定（2026-09-14）**：29 条按四档中的「**降级为概念影响**」落地（写入 `theory_data.js` 覆盖层），不新增 `rel:"retro"` 通道；其余未承认边随 5.2b 的「按主管线重建」一并消解。重建前须先写覆盖层。见 `batches["5.1d"].ruling` |
| 5.2b | **全量管线文案副本审核**：`data_full.js` 对 2,265 个共有节点另持一份独立 `summary`，其中 **1,429 条**与主管线不一致；11,053 个全量独占节点中另有 **21 条**带 `deepen` 同型文案（与主管线 23 条同名 id 变体基本重合，如 `canoe`=`tr_canoe`、`chip`=`ic`），其余约 11,030 条来自 `new_techs.json` 另一语料。**2026-09-14 复核已由取证裁定：`graph.json` 与 `data_full.js` 对共有节点仅 1 条不同（同一份拷贝），且对照 v0.9.23 仅 105 条不一致、v0.9.24 为 971、v0.9.25 为 1,429 → 该侧是主管线在 v0.9.23 时点的快照，**共有节点以主管线为权威**；仍待裁定的仅剩 11,053 个独占节点的语料性质与 23 条 id 变体处置**。见 `audit/ledger.json` 的 `batches["5.2b"]` | 待定 | **已裁定（2026-09-14）**：共有节点以主管线为权威；处置＝以主管线为源**单向重建**，与 5a、5.1d 合并为同一批，执行时点排在阶段 3、4 之后随 5e；重建时须新增「共有节点文案差异 = 0」断言纳入 pre-commit。见 `batches["5.2b"].ruling` |

**5a 的前置取证（2026-09-13，批次 5.0）**：用户抽检 5 条关系（石墨烯→自修复材料、3D 打印→碟式斯特林、忠诚僚机→二维材料 / 摩擦纳米发电 / 可回收火箭），逐层核查证实**5 条全部为位置式伪边**（每条都恰好等于「该分类内年份严格小于依赖者的最后一个节点」，命中率 5/5）。残留位置：主管线 `dependsOn` 0/5（v0.9.20/21 已清）、主管线节点摘要 3/5、中间技术库 `backgrounds` 5/5、**全量管线 graph.json 与 data_full.js 1/5**（`mil_loyalwingman → tr_reusablerocket`，两侧 raw 方向互为镜像、语义相同，`tree_full.html` 仍展示）。取证结论存于 `audit/ledger.json` 的 `batches["5.0"]`。

**5a 已完成部分的判定依据（2026-09-13，批次 5.1c）**：不再依赖启发式检测，改用**既成判定的传播**——以 `git eaa086f^` 与当前主管线逐边 diff，得到该批次判定删除的 **577 条**边，再核验：①其中 **576 条**同时存在于 `graph.json` 与 `data_full.js`（两侧一致，互为交叉校验）；②这 576 条**无一**被主管线重新承认；③其两端节点在 `TECHS_FULL` 中均存在。满足三条即从全量管线两侧移除，不新增任何判定。

> **方法学记录（重要）**：对全量管线使用「位置式签名」启发式扫描，命中数与真实判定集合高度不重叠——上述 576 条真实伪边中，仅 **18 条**同时命中位置式签名。这与批次 1 的教训一致：**该签名只能作筛查信号，不能作判据**；收口的确定性方法是「生成器复现 / 既成判定传播 + 逐边 diff」。

**5b 覆盖不全的根因与补漏判定依据（2026-09-13，批次 5.2a）**：批次 5.1（5b）宣称「重跑为不动点」，但该不动点是**假不动点** —— 它只能证明「工具已识别到的句子不再需要改」，不能证明「所有该改的句子都被识别到」。本轮以一条**与识别器不同源**的断言复核（不依赖识别骨架，直接对全库文本提问「文案声称催生的下游是否真存在于当前反图」），命中 12 处不一致，据此定位两个缺口：① **槽位通配误排空白字符** —— 判定模板句的通配式为 `[^\s，。；、（）()「」]{1,28}`，而库内存在「ABS 树脂」「工业 4.0」「遗传学 / DNA」「金属 3D 打印」等名称内部含空格的节点，凡枚举含此类名称的句子整体失配、被判为手写散文**静默跳过**（不报错，故无暴露窗口）；② **同构生成链未穷举** —— 项目内共三条独立文案生成链，5b 按「文件」而非「生成链」清点，只覆盖族 A，漏掉族 B（`extend_net.js` 的 `deepen()`）与族 C（`gen_1000.js` 的 `summarize()`）。修正后三族合计改写 **531 处 / 461 节点**，且语义断言由 12 处不一致降为 **0**。

> **方法学记录（批次 5.2a 新增，已升格为固定动作）**：**「不动点」不是「完备性证明」。** 同源校验只能证明自洽，无法发现识别器自身的盲区；凡交付「以规则识别 + 重写」为基础的工具，必须附带至少一条**不依赖该规则**的抽查断言。另：族 B 的映射表与族 A **不同**（`extend_net.js` 自带 `ERA_NAME`：`ancient`=上古 / `classical`=古典 / `medieval`=中古 / `future`=未来/科幻，`DOMAIN` 多一个 `future` 键），混用会产生假阳性（复核初期误用族 A 表，出现 80 处「应为古代文明」的假命中）。

---

### 阶段 6 · 维度层（架构改造）

> **定位**：本阶段**不修边**，改造的是网络的**表达框架**。它与阶段 3 的关系是：阶段 3 的每一批都在「逐条把归属边挑出来删掉」，本阶段提供**承载归属的容器**，使「删掉之后信息去哪里」有确定答案（规则 R6 / R7 / R8）。**3.1f 起，处置档位增加第五档 `toField`。**

| 批次 | 内容 | 日期 | 状态 |
|---|---|---|---|
| D0 | 骨架落地 + 全库机械打标：新增维度层覆盖层与校验工具、14 枢纽转维度顶点、执行器加第五档、详情页渲染 | 2026-09-15 | **已完成（v0.9.33）**：`assets/pages/dimension_data.js`（10 门类 / 88 任务域 / 三级兜底）、`tools/gen_dimension.js`；全库 2,267 条打标完成（主维度 + 任务域 + 副维度）；`sub` 与 `category` 同类约束 0 例外；门类分布 eng 857 / life 339 / info 283 / matter 250 / mil 237 / math 126 / phys 90 / symbol 69 / chem 10 / earth 6；**依赖网络零改动**（2,267 / 6,169） |
| D1 | 例外表：把兜底明显不符者逐条写入 `dimension_data.js` 的 `overrides` | 待定 | **草案待审**：目标为门类偏斜（`eng` 37.8% / `chem` 10 / `earth` 6）与 2 个零命中任务域（`energy/animal_human`、`manufact/food_proc`） |
| D2 | 14 个维度顶点的既有入边逐条处置（`delete` / `toField`）；裁定枢纽自身 `dependsOn` 如何落 | 待定 | **草案待审**：入边合计 **2,565** 条（`mathematics` 570 / `chemistry` 433 / `physics` 290 / `algorithm` 214 / `statistics` 194 / `engineering` 170 / `writing` 114 / `optimization` 106 / `numerical` 105 / `molecular_bio` 94 / `materials` 91 / `genetics` 86 / `immunology` 86 / `language` 12）。**待裁定点**：14 个枢纽自身的前置一律为 `writing + mathematics`，二者亦在枢纽名单内 —— 需裁定是否作「维度顶点之间不再互设前置」处理 |
| D3 | A 族 494 条按 `toField` 重判：唯一成立理由是学科归属者落回维度 | 待定 | **草案待审**（规则 R8）。与阶段 3b–3d 的 A 族批次合并推进 |

**维度层的三条轴**：`cat`（节点自带 `category`，9 值 / 100% 覆盖 / 本阶段不改）、`sub`（技术任务域 88 域，单值）、`field`（学科归属 10 门类，8 个锚定 14 个维度顶点；主单值 + 副多值）。

**兜底与例外**：主维度 `override → 维度顶点 → 一段前缀 → category`；任务域 `override → 两段前缀 → 同 category 关键词 → 一段前缀（须同类）→ category 默认域`。**例外一律走 `overrides` 逐条写入，不修改兜底规则本身**（保持规则的机械可复算性）。

---

## 四、批次总览

| 阶段 | 批次数 | 主要对象 | 目标指标 |
|---|---|---|---|
| 0 | 4 | 工具与台账 | 体检可复跑（已完成） |
| 1 | 3 | 27 条边 + 2 个孤立点 + 23 组重复节点 | F 族错误边清零（已完成） |
| 2 | 4 | 中间技术库（原方案撤销 · 改判为性质核查与防再生） | 原「96 条入网」撤销；库内重复清零、生成器加护栏（已完成） |
| 3 | 约 20 | E 族 173 + A 族 563 + Ch 族 260 | A → 150 以下，E → 0，Ch → 120 以下 |
| 4 | 4 | 覆盖矩阵 / 纪元 / 占比 / 反索引 | 纪元取值唯一，反图一致 |
| 5 | 8 | 双管线 / 派生文本（含补漏三族） / 中间技术库对齐 / 生成器 / 上线 | graph.json 与主管线一致；派生文本与当前网络一致（不动点 + 不同源断言）；中间技术库 `backgrounds` 与主管线一致；提交前 3 道门禁全绿 |
| 6 | 4 | 维度层：学科归属（10 门类）+ 技术任务域（88 域）+ 14 个维度顶点 + 第五档 `toField` | 归属有容器可落；`dependsOn` 回归单一语义（必要条件）；3.1f 起归档可用 `toField` |
| 合计 | 约 48 | | |

---

## 五、每批固定动作

1. **判定**：按第二章四标准逐条判定，结论写入 `audit/ledger.json`，脚本断言无漏判；
2. **应用**：只改目标文件，改前备份至 `/tmp`；生成文件用行级编辑，不做整数组重排；
3. **校验**：`tools/audit_net.js --check` 与网络完整性检查（环、悬空、年份倒挂、重边）；站点侧跑全量 runtime 干跑；
4. **同步派生数据（凡改动 `dependsOn` 的批次必做）**：**按依赖顺序**依次重跑 `tools/regen_text.js --apply` → `tools/regen_midtech.js --apply`（中间技术库 `summary` 是主管线摘要的拷贝，逆序会在下游制造二次漂移）；重跑后均须为不动点（输出 0 处变化），否则说明存在未同步的引用；
5. **完备性抽查（凡交付「以规则识别 + 重写」类工具的批次必做）**：除不动点外，须附至少一条**不依赖该识别规则**的断言（如「文案枚举的下游必须在当前反图中」「上游名串必须在 `dependsOn` 名集中」）。理由：同源校验只能证明自洽，**不动点不是完备性证明**（批次 5.2a 的教训）；
6. **登记**：按需同步 changelog、纠错总结、版本迭代日志；
7. **提交**：一批一提交，提交信息注明族别、处置数与删改留计数；提交前 `pre-commit` 会自动跑 3 道门禁（见第 8 条）；
8. **门禁（三道，任一不过即阻断提交）**：① 文档一致性 `tools/check_docs.js`；② 派生文案不动点 `tools/regen_text.js --check`；③ 结构指标不劣化 `tools/audit_net.js --check`。钩子本身不随仓库版本化，故版本化副本存于 `tools/git-hooks/pre-commit`，克隆后用 `sh tools/install_git_hooks.sh` 安装。确需跳过用 `git commit --no-verify`，但须在版本日志说明理由；
9. **推送**：由用户在 GitHub Desktop 执行。

---

## 六、当前批次

**阶段 5 的部分批次已提前执行完毕（批次 5.1）**，登记版本 v0.9.24。原因：用户在批次 5.0 取证后要求「把所有类似问题都改了」，而 5b / 5d / 5a 之一三项的依赖条件（主管线 `dependsOn` 已定）当时即已满足，且均为**从当前网络重算派生数据**的机械操作，不改变任何因果结构判定，故提前收口。

- **5b 派生文本同步**：977 个节点、**3,313 处**（`summary` 953 + `significance` 940 + `views[].text` 1,420）。工具 `tools/regen_text.js`，按 `upgrade_stubs.js` 的原公式重算（`upsStr = dependsOn 前 3 项`、`downsStr = 反图 children 前 3 项`、模板选择 = `hash(id) % 2` 与 `downs` 是否为空）。**只替换被识别为模板句的句子**，1,724 处手写散文全部未动。重跑为不动点（0 处）。（该不动点后被证明**不完备**，见 5.2a）
- **5d 中间技术库对齐**：`backgrounds` **84 条**、`summary` **69 条**、`born` 5 条变化；95 条目录集合保持不变（**不重选案例**——`gen_midtech.js` 当前重跑只能选出 32 条且 `life` 占 14 条，属口径失效而非数据问题）。规则抽为 `tools/midtech_rules.js`，`gen_midtech.js` 与 `regen_midtech.js` 共用，重跑生成器输出与重构前逐字节一致。
- **5a 之一 全量管线遗留边同步**：576 条（`graph.json` 7,539 → 6,963；`EDGES_FULL` 17,387 → 16,811），节点数不变；`graph.json meta.totalTechs` 一并由 2,288 修正为 2,265（1c 批次重名合并后未同步的陈旧值）。

**批次 5.2 已完成（派生文案补漏），登记版本 v0.9.25**。起因是对 5b 的独立复核暴露其「不动点」为假不动点：

- **5.2a 派生文案补漏（三族）**：**531 处 / 461 节点**。① 修模板识别器盲区（槽位通配排除空白字符 → 含空格名称所在整句被判为手写散文静默跳过），族 A `assets/techs_extra.js` 补改 **108 处 / 38 节点**；② 补覆盖两条此前完全未纳入的生成链 —— 族 B `assets/techs_extend.js`（`extend_net.js` 的 `deepen()`，**34 处**，只重写尾段、手写 base 逐字保留）、族 C `assets/techs_more.js`（`gen_1000.js` 的 `summarize()`，**389 处**）。规则抽为 `tools/derived_text_rules.js` 单一真相源；`regen_text.js` 升级为三族三文件并新增 `--check`。连带中间技术库摘要拷贝同步 **15 条**（派生链顺序：主管线文案 → 中间库文案）。语义断言（与识别器不同源）由 12 处不一致降为 **0**；三族重跑均为不动点（0 处），中间库 95/95 不动点。
- **门禁加固**：`.git/hooks/pre-commit` 由 1 道扩为 3 道（文档一致性 / 派生文案不动点 / 结构指标不劣化）。原钩子备份于 `/tmp/pre-commit.bak.*`。

**已裁定（2026-09-14，v0.9.26）——两项同属全量管线侧，合并为同一批处置**：

1. 批次 **5.1d** —— 全量管线仍有约 **1,116 条**边未被主管线承认（`dependsOn ∪ enables` 口径；仅按 `dependsOn` 判为 1,207）。其中 `yearInv` **29 条**全部为 `enables` 类型，**2026-09-14 已逐条核实**：29 条全部可解读为「该技术的后期阶段吸收了源技术」（`采矿` 用现代炸药爆破、`犁` 用铁器、`拱与穹顶` 用水泥、`电子计算机` 用集成电路 / 半导体、`自动化` 用工业机器人与 AI、`飞机与航天器` 用复合材料、`扫地机器人` 与 `自动驾驶` 用计算机视觉），**故并非方向性错误、不应按伪边删除**。真正的冲突在建模口径：节点 `year` 取「该技术的最早实例」，而边描述的是该技术后期阶段的演进，与主管线 `yearInv=0`（前置年 ≤ 本节点年）硬约束不相容。三种候选表达方式：① 新增 `rel:"retro"` 边类型并放行年份倒挂；② 不建边，改由 `theory_data.js` 覆盖层文字标注（改动面最小、与既有「只读覆盖层、不动网络」模式一致）；③ 拆节点为阶段（如「钢铁（块炼铁）」「钢铁（转炉钢）」）使年序自然成立。用户已确认「不应删除」。**裁定：按既有四档处置中的「降级为概念影响」落地（不写入 `dependsOn`，仅见于文案），载体为 `assets/pages/theory_data.js` 覆盖层。** 不新增 `rel:"retro"` 边类型——新增边类型等于在四标准之外另开一个无判据通道，而本项目的「位置式伪边」正是无约束生成规则的产物；`rel:"retro"` 保留为已登记的方法学待办，若启用须先扩大适用范围到「后出现者促成先出现者的后续演进」并先立判据。**重建前必须先写入覆盖层，否则重建时信息丢失。** 其余未承认边不再逐条判定，随 5.2b 的「按主管线重建」一并消解（重建产物只保留主管线承认的边；重建后须与现有两文件逐边 diff，确认删除集合全部来自主管线不承认的边、且无新增）。节点粒度（拆阶段）另立独立议题。
2. 批次 **5.2b** —— `data_full.js` 对 2,265 个共有节点另持一份独立 `summary`，其中 **1,429 条**与主管线不一致（原记 971 条已被 v0.9.25 的改写作废）；11,053 个全量独占节点中另有 **21 条**带 `deepen` 同型文案。**2026-09-14 复核已由取证裁定权威关系**：①`graph.json` 与 `data_full.js` 对共有节点仅 1 条不同 → 两侧是同一份拷贝，非两份独立语料；②对照主管线历史版本，v0.9.23 不一致 **105** 条、v0.9.24 **971** 条、v0.9.25 **1,429** 条 → 该侧是主管线在 **v0.9.23 时点**的快照，此后每轮主管线文案清理都使差距扩大；③`SCORE_META.generatedAt` = 2026-09-12T13:23:39Z，与快照时点吻合。**故共有节点以主管线为权威，同步方向 = 按主管线重算该侧文案**；**裁定：共有节点以主管线为权威，处置改为以主管线为源单向重建 `graph.json` 与 `data_full.js`（不作逐条打补丁）；执行时点排在阶段 3、4 之后、随阶段 5e 执行，并与 5a「按主管线重建」、5.1d 边处置合并为同一批；重建时须同步新增断言「共有节点文案差异 = 0」并纳入 `pre-commit`（全量侧无门禁是本轮暴露的根因——派生数据与源的差异不引起任何校验失败，故每轮清理都静默扩大漂移）。** 11,053 个独占节点（约 11,030 条来自 `new_techs.json`）判为**独立语料、不并入主管线**，但须加 `provenance`（`mainline-derived` / `new_techs`）并在 `tree_full.html` 给出图例说明来源——未经验证 ≠ 已验证；是否另设独立视图单列一批。23 条同名 id 变体（`canoe`=`tr_canoe`、`chip`=`ic`、`neural_network`=`inf_nn`…）**保留为独立节点、不合并**，改为加 `aliasOf` 字段（合并会改变全量节点数与边数，触发 `check_docs` 的 `TOTAL_FULL` 与 `tree_full.html` 写死数字同步，收益远小于代价）。

> 合并处置已确定：5.1d 与 5.2b 属同一侧（全量管线）、同一权威关系问题，**合并为一批**，由 5.2b 裁定的「以主管线为源单向重建」一次解决（边的未承认与文案的陈旧同源——都出自「导出快照未随主管线重算」）。执行时点排在阶段 3、4 之后，避免阶段 3/4 改动 `dependsOn` 后重复工作。**本轮（v0.9.26）只裁定与登记，未改动任何数据。**

**阶段 3.1a 已执行（2026-09-14，v0.9.28）**：E 族 manufact 首批 27 条（机床族 17 + 焊接 / 连接族 10）逐条判定并写盘 —— 保留 2 / 改前置 1 / 删边 24 / 降级 0，补前置 27 条（+56 条边）；依赖边 6,094 → **6,126**，节点 2,266 不变。判定与理由见 `audit/ledger.json` 的 `batches["3.1a"]`（含候选筛选断言：27 条全部满足「单前置且前置 = `mathematics`」）。**下一批：阶段 3.1b —— E 族 manufact 余 41 条。** 前置形态与 3.1a 完全相同（单前置且前置 = `mathematics`），处置规则 R1–R4 直接沿用。**41 条判定草案已写入** `audit/ledger.json` 的 `batches["3.1b"]`（保留 0 / 改前置 3 / 删边 38 / 降级 0，补前置 41 条），并已通过预演校验（前置存在性 0 错、年份倒挂 0、无环；预计边 6,126 → 6,167、`mathematics` 前置 611 → 570、零前置 4 不变）。**已写盘（2026-09-14，v0.9.30）**：保留 0 / 改前置 3 / 删边 38 / 降级 0，补前置 41 条目共 79 条边；移除 41 条 `mathematics`、实际新增 82 条，依赖边 6,126 → **6,167**，节点 2,266 不变。**保留数为 0 是本批的判据性结论**：该槽位不区分必要与非必要，属批量赋值为学科级顶点的产物；四标准中的排他性是筛出它们的唯一判据。

**下一批：阶段 3.1c 草案已出（2026-09-14，未写盘）** —— 取 E 族 info 91 条中的「通信、网络与标识设备」一簇 18 条（`inf_cable` 通信电缆 / `inf_antenna` 天线 / `inf_phasedarray` 相控阵 / `inf_microwave` 微波中继 / `inf_rfid` 射频识别 / `inf_fiber` 光纤通信 / `inf_alloptical` 全光网络 / `inf_400g` 高速光模块 / `inf_router` 路由器 / `inf_sensor_net` 无线传感网 / `inf_cdn` 内容分发网络 / `inf_bluetooth` 蓝牙 / `inf_vpn` 虚拟专网 / `inf_sdn` 软件定义网络 / `inf_leo` 低轨卫星星座 / `inf_5g` 5G / `inf_barcode` 条形码 / `inf_qrcode` 二维码），逐条判定写入 `audit/ledger.json` 的 `batches["3.1c"]`：保留 **0** / 改前置 **1**（`inf_antenna` → `em_theory` 电磁理论，其定义性构成本身即学理）/ 删边 **17** / 降级 0，补前置 17 条目共 37 条边。预演校验通过（前置存在性 0 错、年份倒挂 0、无环、零前置 4 不变）；预计依赖边 6,167 → **6,187**（移除 18 + 新增 38 = `backfills` 37 + `newParent` 1）、`mathematics` 前置 570 → 552、A 族 494 → 476、E 族 105 → 87。**待用户逐条审核后写盘。**

**后续批次 3.1d 草案亦已出（2026-09-15，未写盘）** —— 取 E 族 info 中「信息记录、存储与音视频设备」一簇 16 条（`inf_carbonpaper` 复写纸 / `inf_typewriter` 打字机 / `inf_vinyl` 黑胶唱片 / `inf_tape` 磁带 / `inf_color_tv` 彩色电视 / `inf_vcr` 录像机 / `inf_hdd` 硬盘 / `inf_floppy` 软盘 / `inf_camcorder` 摄像机 / `inf_flash` 闪存 / `inf_emap` 电子地图 / `inf_dataviz` 数据可视化 / `inf_navsoft` 导航软件 / `inf_watermark` 数字水印 / `inf_ssd` 固态硬盘 / `inf_usb` U 盘），判定写入 `batches["3.1d"]`：保留 **0** / 改前置 **2**（`inf_vinyl` → `acoustics` 声学、`inf_dataviz` → `statistics` 统计学）/ 删边 **14** / 降级 0，补前置 16 条目共 30 条边。预演校验通过（0 错、无倒挂、无环、零前置 4 不变）；预计依赖边 6,167 → **6,183**（移除 16 + 新增 32）、`mathematics` 前置 570 → 554。**与 3.1c 节点集合不相交，写盘顺序不限。待用户逐条审核后写盘。**

**后续批次 3.1e 草案亦已出（2026-09-15，未写盘；已按用户裁定修订）** —— 取 E 族 info 中「基础软件与数据管理」一簇 18 条（`inf_microprocessor` 微处理器 / `inf_os` 操作系统 / `inf_compiler` 编译器 / `inf_pl_fortran` Fortran / `inf_pl_c` C 语言 / `inf_pl_python` Python / `inf_middleware` 中间件 / `inf_uml` 统一建模语言 / `inf_db` 数据库 / `inf_rdbms` 关系型数据库 / `inf_sql` SQL / `inf_nosql` NoSQL / `inf_graphdb` 图数据库 / `inf_timeseries` 时序数据库 / `inf_vector_db` 向量数据库 / `inf_cache` 缓存系统 / `inf_compression` 数据压缩 / `inf_hash` 哈希函数），判定写入 `batches["3.1e"]`：保留 **0** / 改前置 **1**（`inf_compression` 数据压缩 → `info_theory` 信息论）/ 删边 **17** / 降级 0，补前置 18 条目共 33 条边。预演校验通过（0 错、无倒挂、无环、零前置 4 不变）；预计依赖边 6,169 → **6,185**（移除 18 + 新增 34）；`mathematics` 前置 570 → 552、A 族 494 → 476、E 族 105 → 87、Ch 族 246 → **243**（`graph_theory` / `math_logic` / `number_theory` 首次获得下游）。**三批（3.1c / 3.1d / 3.1e）节点集合两两不相交，写盘顺序不限。**（三批全部写盘后累计口径相应为：以写盘前的实际盘面为基准累加。)

**本批按 2026-09-15 用户裁定的三处修订**：② `inf_hash` 哈希函数由 `math_logic` 数理逻辑改为 **`number_theory` 数论(-300)**（哈希靠模运算 / 素数取模）；③ `inf_compiler` 编译器由 `math_logic` 改为 **`lambda_calc` λ演算(1936)**（编译器的理论根是形式语言 / 可计算性，库内实测无该类节点，λ演算为最贴近的可用学理）；④ `inf_uml` 统一建模语言由 `computer_sci` 计算机科学改为 **`inf_oop` 面向对象方法(1967，本轮新建)** + `inf_pl_c`。① `inf_compression` → `info_theory` 改指、⑤ `inf_vector_db` 同年前置放行、⑥ `computer` 下游 214 → 221 三处均经用户确认。**新增判定规则 R5：不得以学科级顶点充当替身前置。**

**补节点批次 `node-add-20260915-oop` 已执行（2026-09-15，v0.9.32，阶段序列外）** —— 起因是 3.1e 复核点 ④：`inf_uml` 的真实来源是**面向对象方法**，而网络内无此节点，草稿一度以学科级 `computer_sci` 代位。用户裁定「**这类错误的原因是缺失合适的节点，解决方案是新建节点**」。经联网取证补建 `inf_oop` 面向对象方法（`info` / `era=info` / 1967 / 挪威·奥斯陆 / Dahl 与 Nygaard）：**取 1967**（Simula 67 首次把封装、继承 / 后绑定、动态对象生成三特征合并为同一套机制），不取 1961（合作开始）或 1965（Simula I 无继承）；前置 2 条（`inf_compiler` 1952 + `inf_pl_fortran` 1957）；排除 `lambda_calc`（排他性不成立）、`set_theory`（偏泛）、`inf_pl_c`（1972 晚于 1967）、`computer_sci`（学科级顶点禁用）。写盘后节点 2,266 → **2,267**、依赖边 6,167 → **6,169**；派生链 `regen_text` 6 处 / 2 节点、`regen_midtech` 95/95 未变化，均复跑不动点；三道门禁全绿；基线已刷新。**同时把「以不贴切的替代充当前置」总结为一类错误（形态一：批量退回学科级顶点；形态二：以一个学科级顶点替换另一个；形态三：以泛泛学理替换具体学理；根因＝节点缺失，危害在于形态二、三的边看起来「有具体前置」却过不了排他性反问），登记入纠错页与版本日志。** 其余缺失节点累计 **17 项**列入补节点候选。

**阶段 6 · 维度层（架构改造）已启动（2026-09-15，v0.9.33，批次 D0）** —— 完成骨架落地与全库机械打标，**依赖网络零改动**。裁定与设计见 `audit/ledger.json` 的 `batches["D0"]`。

- **本阶段要解决的问题（模型层）**：主管线 `dependsOn` 是单一平面的有向图，只有「前置」一个槽位，于是「这个节点属于哪个学科 / 领域」这种**归属**关系无处安放、只能被写成一条边 —— 14 个泛化学理枢纽因此各自成为巨型扇入节点，入边合计 **2,565 条**。一种关系被迫承担两种语义（必要条件 / 学科归属），判据不同而槽位相同，归属只能伪装成前置。这解释了阶段 3 反复遇到的现象：E 族四批 93 条**保留数全为 0**，且这些边**必然通过时序校验**（学科级节点总是更早）、**只能靠排他性筛出** —— 若只在平面模型里逐条删边，症状会减少但成因不变。
- **模型层内部矛盾（本轮裁定）**：阶段 3 判「删边」，而 `theory_data.js` 显示层已上线的是「保边 + 标 `theory.latent` 虚边」（注释原文「虚边徽章 = 潜在支撑（背景性学理，非直接造物所需）」）。**二者不能同时成立** —— 边删掉了就无法再显示为虚边。裁定走「**删边 + 归属标签**」，`kinds` 机制收窄为只标**真正必要但背景性**的学理边（如 3.1c 已裁定保留的 `inf_compression ← info_theory`）。
- **六项设计（用户 2026-09-15 逐条确认）**：① 两条轴并存（`sub` 技术任务域 / `field` 学科归属）；② 主维度单值 + 副维度多值；③ 14 个泛化枢纽全部转「维度顶点」；④ 同意「`id` 前缀 + `category` 机械兜底 + 例外逐条覆盖」；⑤「学理」并入学科维度、不独立成维；⑥ 四档改五档（+ `toField`）。
- **落地物**：`assets/pages/dimension_data.js`（10 学科门类 / 88 任务域 / 三级兜底 / `overrides` 例外表 / `resolve()`）、`tools/gen_dimension.js`（4 项注册表断言 + 4 项全库断言 + 报表）、`tools/apply_ledger.js` 第五档、`assets/core.js` 的 `buildDimensionBlock`、`assets/style.css` 的 `.dim-*`、16 页挂载 + 版本串 `20260915a`、台账批次 `D0`。
- **机械兜底结果**：主维度路径 prefix 1,838 / cat 255 / prefix:bx 160 / hub-head 14；任务域路径 p2 826 / kw 809 / p1 460 / catSub 172。门类分布 `eng` 857（37.8%）/ `life` 339 / `info` 283 / `matter` 250 / `mil` 237 / `math` 126 / `phys` 90 / `symbol` 69 / `chem` 10 / `earth` 6。**偏斜与 2 个零命中任务域（`energy/animal_human`、`manufact/food_proc`）是机械兜底的已知局限**，由 D1 例外表逐条修正，不得据此认为打标已经准确。
- **两处约束收紧**：关键词匹配限定在同 `category` 的域内（跨类匹配是唯一系统性噪声源）且关键词长度 ≥ 3；一段前缀兜底须与 `category` 同类。修正前 24 项 `sub` 与 `category` 不同类，修正后 **0 例外**。
- **校验**：`gen_dimension.js` 全部断言通过；渲染校验 `/tmp/render_check_0933.js` **38 / 38**；三道门禁全绿；基线未刷新（无合法改进可刷）。
- **待审三项**：**D1** 例外表 —— 把兜底明显不符者逐条写入 `overrides`；**D2** 14 个维度顶点入边逐条在 `delete` 与 `toField` 之间判定，并裁定「枢纽自身的 `dependsOn`（`writing + mathematics`）如何落」；**D3** A 族 494 条按 `toField` 重判（与阶段 3b–3d 合并推进）。

**本阶段新增流程（首次启用）**：语义判定批次改为**「先出判定草案、再执行」**——先在台账写入每条的 `verdict` + `newParent` + `reason`，交审核后再用执行器写盘。原因是 E / A 族修复直接改写知识图谱的因果结构，判定质量依赖逐条复核，不宜边判边改。

**本阶段新增流程（批次 5.1 建立）**：派生数据（文案 / 中间库背景 / 全量管线边）一律**以主管线为准重算**，工具化并可重跑（`regen_text.js` / `regen_midtech.js` / `prune_full_pipeline.js`）。因此**后续任何改动 `dependsOn` 的批次，收尾必须重跑前两个工具**，否则文案与背景会再次漂移——这是本批次暴露出的系统性风险，已写入 §五 固定动作第 4 条。

**本阶段新增流程（批次 5.2a 建立）**：① **完备性抽查** —— 交付「以规则识别 + 重写」类工具时，除不动点外必须附一条不依赖该规则的断言（§五 第 5 条）；② **派生链顺序** —— 重算必须按「主管线文案 → 中间库文案」的顺序执行（§五 第 4 条）；③ **门禁前置** —— 三道门禁已自动运行于每次提交，不再依赖人工记得跑（§五 第 8 条）。

**审核节点**：
- 阶段 1（止血批）：删边 15 条、改前置 4 条、合并重复节点 23 组，已完成待确认。
- 阶段 2（性质核查批）：原方案撤销、重复条目修复、生成器加护栏，已完成待确认。
- 阶段 5.1（派生数据同步批）：文案 3,313 处、中间库 84 + 69 条、全量管线 576 条，已完成待确认。
- 阶段 5.2（派生文案补漏批）：三族 531 处 / 461 节点、中间库摘要 15 条、门禁扩为 3 道，**已完成待确认**；**两项待审已裁定**（5.1d 约 1,116 条边 · 5.2b 1,429 条文案）：2026-09-14 完成三条取证并裁定「共有节点以主管线为权威」，处置为「以主管线为源单向重建」并入阶段 5e；29 条年份倒挂降级为概念影响（先写 `theory_data.js` 覆盖层）；11,053 个独占节点判为独立语料、加 `provenance` 标注；23 条 id 变体加 `aliasOf` 而不合并。登记版本 v0.9.26。
- 阶段 3（语义判定批）：**3.1a 已完成并写盘（v0.9.28）** —— E 族 manufact 首批 27 条（保留 2 / 改前置 1 / 删边 24 / 补前置 27，依赖边 6,094 → 6,126；A 族 562 → 535、E 族 173 → 146、Ch 族 260 → 252；派生文案 45 节点 136 处 + 中间库 3 条，三道门禁全绿）。**3.1b 已完成并写盘（v0.9.30）** —— E 族 manufact 余 41 条逐条判定（保留 0 / 改前置 3 / 删边 38 / 补前置 41，实际新增 82 条边），依赖边 6,126 → **6,167**；A 族 535 → 494、E 族 146 → 105（E 族 manufact 槽位清空）、Ch 族 252 → 246；派生文案 191 条 + 中间库 95 条目，三道门禁全绿。**3.1c 草案已出（2026-09-14，待审核）** —— E 族 info 首批 18 条（通信、网络与标识设备）逐条判定：保留 0 / 改前置 1（`inf_antenna` → `em_theory`）/ 删边 17 / 补前置 17 条（37 条边）；预演依赖边 6,167 → **6,187**、`mathematics` 前置 570 → 552、A 族 494 → 476、E 族 105 → 87。**待用户逐条审核后写盘。** **3.1d 草案亦已出（2026-09-15，待审核）** —— E 族 info 次批 16 条（信息记录、存储与音视频设备）逐条判定：保留 0 / 改前置 2（`inf_vinyl` → `acoustics`、`inf_dataviz` → `statistics`）/ 删边 14 / 补前置 16 条（30 条边）；预演依赖边 6,167 → **6,183**、`mathematics` 前置 570 → 554。**待用户逐条审核后写盘。** **3.1e 草案亦已出（2026-09-15，待审核；已按用户裁定修订）** —— E 族 info 三批 18 条（基础软件与数据管理）逐条判定：保留 0 / 改前置 1（`inf_compression` → `info_theory`）/ 删边 17 / 补前置 18 条（34 条边）；预演依赖边 6,169 → **6,185**、`mathematics` 前置 570 → 552、A 族 494 → 476、E 族 105 → 87、Ch 族 246 → **243**。裁定后修订三处：② `inf_hash` → `number_theory`、③ `inf_compiler` → `lambda_calc`、④ `inf_uml` → 新建节点 `inf_oop`（补节点批次 `node-add-20260915-oop` 已于 2026-09-15 写盘，节点 2,266 → 2,267 / 边 6,167 → 6,169，v0.9.32）。**待用户逐条审核后写盘。** 后续批次：E 族其余（info 余 39 = 密码与安全 8 + 云计算与网络软件 12 + 算法与 AI 18 + 开发方法 1、life 5、其余 6）或 A 族。

**阶段 6 · 维度层（架构改造）已启动（2026-09-15，v0.9.33，批次 D0）—— 依赖网络零改动**：新增只读覆盖层 `assets/pages/dimension_data.js`（10 学科门类 / 88 技术任务域 / 三级兜底 / 例外表）与校验工具 `tools/gen_dimension.js`，全库 2,267 条完成机械打标（主维度 + 任务域 + 副维度，`sub` 与 `category` 同类约束 0 例外），14 个泛化枢纽全部转为「维度顶点」，处置档位由四档改**五档**（新增 `toField` ＝ 删伪边 + 落回维度），详情页新增「维度归属」渲染。裁定走「删边 + 归属标签」（与 `theory_data.js` 已上线的「保边 + 标 `latent` 虚边」不能同时成立）。三道门禁全绿、渲染校验 38/38。**待审三项**：**D1** 例外表（门类偏斜 `eng` 37.8% / `chem` 10 / `earth` 6 与 2 个零命中任务域）、**D2** 14 个维度顶点入边逐条处置（合计 **2,565** 条；并需裁定「枢纽自身的 `dependsOn`＝`writing + mathematics` 如何落」）、**D3** A 族 494 条按 `toField` 重判。**3.1c / 3.1d / 3.1e 三批草案的结论不受影响**（其删的是「技术节点挂到学科级顶点」的伪边，两种模型下判定相同），仍待用户逐条审核后写盘；**3.1f 起**可启用第五档 `toField`。
